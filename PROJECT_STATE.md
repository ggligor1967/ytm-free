# PROJECT_STATE — YTM Free

> Single source of truth for "what actually works right now".
> Every claim below is dated and backed by a command. When you re-verify, update the date and result — never leave a stale ✅.
> If this file disagrees with any other doc (README, docs/FAZA_*, CHANGELOG), **this file wins**; update the other doc or flag it.

**Last full verification:** 2026-07-06 (Claude Code session, Windows 11, local machine)
**Step-1D toolchain shell remediation (2026-07-08):** the remaining plain-shell `dbghelp.lib` blocker is resolved for the normal PowerShell workflow on this machine. A user-level environment pin plus PowerShell bootstrap (`C:\Users\gglig\Documents\PowerShell\profile.ps1`) now auto-selects MSVC + Windows SDK `10.0.26100.0` without manual `vcvarsall.bat`. Evidence: from a normal PowerShell shell, `. C:\Users\gglig\Documents\PowerShell\profile.ps1; cargo test --no-run` compiled successfully in `src-tauri`, and full `. C:\Users\gglig\Documents\PowerShell\profile.ps1; cargo test -- --test-threads=1` passed `34/34`. The root cause on disk is unchanged: SDK `10.0.28000.0` is still partial (115 libs, no `dbghelp.lib`), so non-PowerShell or env-less raw shells may still fail if they bypass the bootstrap.
**Step-3B release-runtime smoke (2026-07-07, eleventh session):** see the Step-3B paragraph below - built release executable NOT launched (BLOCKED-RUNTIME-ISOLATION); artifacts verified present and hashed.
**Step-1 re-verification (same day, second session):** the `dbghelp.lib` toolchain block is **bypassable** via a session-only SDK pin — see "Verified BROKEN" table below for the new finding and the changed blocker.
**Step-1B re-verification (same day, third session):** the disk-space blocker is **resolved** (C: now 11.2 GB free). With the SDK 10.0.26100.0 pin, `cargo test` now **compiles and runs** (Finished in 35.98s, only warnings) — but reveals **8 real failures in `db::tests::*`** (20 passed / 8 failed of 28). Rust is no longer BLOCKED-ENV; it is **PASS-COMPILE-BUT-TEST-FAIL**. Root cause is a pre-existing bug in `run_migrations` (version-0 baseline collision), not the toolchain. See "Verified BROKEN" table.
**Step-1C fix verification (same day, fourth session):** the `db::tests` blocker is **resolved** on branch `fix/db-initial-migration-baseline`. `run_migrations` now treats an empty `schema_migrations` table as baseline `-1`, so migration version 0 runs on fresh databases while preserving existing migration history. With the SDK 10.0.26100.0 pin, `cargo test db::tests` → 8 passed and full `cargo test` → 28 passed. Frontend Gate A also passed: `npx tsc --noEmit` exit 0, `npm test` → 32 passed, `npm run build` → built in 6.98s.
**Step-2A runtime startup smoke (same day, fifth session):** first controlled Tauri dev startup is **verified** with the SDK 10.0.26100.0 pin. `npm run tauri dev` started Vite on `http://localhost:5173`, launched `target\debug\ytm-free.exe` with responding window title `YTM Free`, and the Axum stream server listened on `127.0.0.1:3456`; `curl.exe -i http://localhost:3456/health` → `HTTP/1.1 200 OK` / `OK`. This is startup evidence only, not a full search/stream/download/persistence e2e.
**Step-2B minimal non-destructive smoke (same day, sixth session):** startup remained **PASS**, but the first real read-only stream probe is **FAIL-E2E**. Under the same SDK pin, `yt-dlp --dump-json --flat-playlist "ytsearch10:test"` returned ordinary short-form results, but probing the app's local stream route with sampled IDs (for example `curl.exe -i http://localhost:3456/stream/YXZH-eBtmqQ`) returned `HTTP/1.1 500 Internal Server Error` and `Failed to get audio: Failed to execute yt-dlp: ERROR: [youtube] ... Requested format is not available`. This means startup/server/window are verified, but a minimal user-facing search-to-stream path is still not verified.
**Step-2B stream fix verification (same day, seventh session):** the minimal read-only stream smoke is **resolved on branch `fix/stream-audio-url-resolution`**. `ytdlp::get_audio_url` now asks `yt-dlp` for `bestaudio/best` instead of strict `bestaudio`, preserving the audio preference while falling back when YouTube exposes only a combined playable format. Evidence: current selector `yt-dlp -f bestaudio -g --no-warnings https://www.youtube.com/watch?v=YXZH-eBtmqQ` reproduced `Requested format is not available`; `bestaudio/best` returned a direct URL. With the patch, `curl.exe -i http://localhost:3456/stream/YXZH-eBtmqQ` returned `HTTP/1.1 307 Temporary Redirect` instead of 500. No download/import/delete/mutation flow or production build was run.
**Step-3A production build attempt (same day, eighth session):** first controlled
pm run tauri build attempt is **BLOCKED-ENV at the disk decision gate** — not run. Preflight: local main == origin/main == f8bb649621ea44a33b7aec31175bf3078d5fc44, branch main, only the known untracked files present (.omx/, GDPR drafts). Toolchain verified: Node v22.22.2, npm 11.7.0, rustc/cargo 1.94.1, yt-dlp 2026.02.04; SDK 10.0.26100.0 dbghelp.lib and cvarsall.bat both present. Disk: sutil volume diskfree C: → Total free bytes 9,342,238,720 (**8.70 GB free**), down from 11.2 GB in the 3rd session. Existing build artifacts: src-tauri/target = **10.62 GB**, dist = ~0.4 MB. The Step-3A gate requires ≥15 GB free before running
pm run tauri build; 8.70 GB < 15 GB, so per the recovery protocol the production build was **not executed** and no source was changed. Production build command (for reference):
pm run tauri build → 	auri build, which auto-runs eforeBuildCommand (
pm run build) and emits release artifacts under src-tauri/target/release/ + src-tauri/target/release/bundle/ (undle.targets: "all" → MSI + NSIS on Windows). Classification: **BLOCKED-ENV** (insufficient disk space). No baseline gates or production build were run this session because the disk gate fires before the build. Cleanup verified: no listeners on 3456 or 5173; git status --short unchanged (only known untracked files); main/origin/main unchanged at f8bb649621ea44a33b7aec31175bf3078d5fc44.

**Step-3A re-run after PASS-PREP (same day, ninth session):** the disk gate was cleared on main (local main == origin/main == `f71a946fdc49bd34f2ca740bd4ac81de1132d224`; C: `24.03 GB` free; `src-tauri/target` absent; `dist` absent), but the required first baseline gate failed before any production build could run. In an SDK-pinned shell (`vcvarsall.bat x64 10.0.26100.0` imported into the session), `cd src-tauri && cargo test` exited 101 because `tauri::generate_context!()` panicked: `The frontendDist configuration is set to "../dist" but this path doesn't exist`. This is a clean-workspace repo/config coupling in the baseline gate, not a disk-space or SDK failure. `npm run tauri build` was **not executed**, no source was changed, and the failed cargo run recreated a partial `src-tauri/target` (~2.05 GB) while `dist` remained absent. Classification: **FAIL-BUILD**.
**Step-3A corrected-order verification (same day, tenth session):** the first controlled production Tauri build is **verified** on main with the corrected clean-workspace baseline order. Preflight: local main == origin/main == `9bde65010bb4b239224bd012969e603978ec8dcb`, branch `main`, only the known untracked files present, C: `22.10 GB` free, `dist` absent, partial `src-tauri/target` `2.05 GB`, SDK `10.0.26100.0` `dbghelp.lib` and `vcvarsall.bat` present. Baseline gates passed in order: `npx tsc --noEmit` exit 0; `npm test` → 32/32 passed in 8.19s; `npm run build` → rebuilt `dist` (0.44 MB) in 4.36s; SDK-pinned `cd src-tauri && cargo test` → 28 passed, 0 failed. Then SDK-pinned `npm run tauri build` exited 0, produced `src-tauri/target/release/ytm-free.exe`, `src-tauri/target/release/bundle/msi/YTM Free_0.1.0_x64_en-US.msi` (3.63 MB), and `src-tauri/target/release/bundle/nsis/YTM Free_0.1.0_x64-setup.exe` (2.67 MB). Post-build C: free was `18.30 GB`; no listeners remained on 3456 or 5173; no source changed; no generated artifacts were staged, installed, launched, uploaded, distributed, or committed. Classification: **PASS-BUILD**. Full release-runtime smoke, download flows, restart, and persistence remain unverified.

**Step-3B release-runtime smoke (2026-07-07, eleventh session):** the built release executable was **not launched** because its startup data path writes to the real user AppData with no safe isolation available. Preflight on `main` confirmed local main == origin/main == `461e761bd5fcde6e5d225a423bb348e9efe2103d` (branch `main`; `git rev-list --left-right --count origin/main...main` -> 0	0), only the known untracked files present (`.omx/`, `gdpr-compliance-audit-report.md`, `docs/GDPR_REMEDIATION_PLAN.md`, `docs/plan-remediere-gdpr-complete.md`), and nothing staged (`git diff --cached --name-only` empty; `git status --short` shows only the known untracked files). Artifacts confirmed present and hashed (sizes/SHA256 captured this session): `src-tauri/target/release/ytm-free.exe` = 8,760,832 bytes, SHA256 `9E30ED8C2E7B70F61E2D32825CD68DE03D0D17FB896BBC0124B042F339950B3C`; `src-tauri/target/release/bundle/msi/YTM Free_0.1.0_x64_en-US.msi` = 3,805,184 bytes, SHA256 `679ED84143941BF51DB9C33D35AB4BB4E75403D225A2627E01FC3290A4ACA62D`; `src-tauri/target/release/bundle/nsis/YTM Free_0.1.0_x64-setup.exe` = 2,801,097 bytes, SHA256 `B5A0F2AA341E0BCC176E5B3520D4D0EEA654B6E91C056FEAF56EF6CCE37233E0`. Runtime data-path inspection (`src-tauri/src/db.rs:235-240`, `src-tauri/src/lib.rs:3211-3213`, `src-tauri/src/main.rs`): the Tauri `setup` hook unconditionally calls `Database::new()`, whose `get_db_path()` hardcodes `dirs::data_dir().unwrap_or_else(|| PathBuf::from(".")).join("ytm-free").join("ytm-free.db")`. The dependency is `dirs = "5"` (`src-tauri/Cargo.toml`); on Windows `dirs` v5 `data_dir()` resolves `FOLDERID_RoamingAppData` via `SHGetKnownFolderPath` (reads the user profile/registry, **not** the `$env:APPDATA` variable), so overriding `APPDATA` does not redirect the DB path. `main.rs` parses no CLI arg for a data dir and sets no override; the code uses no Tauri app-handle path-resolver override. A real DB already exists at `C:\Users\gglig\AppData\Roaming\ytm-free\ytm-free.db` (1,482,752 bytes, last modified 2026-07-06, from prior dev smoke runs), so launching would open/mutate real user AppData (SQLite `Connection::open` + `run_migrations` guard + possible WAL/journal writes) with no safe isolation and without modifying source (forbidden this session). Per Operating Rule 10 the release executable was therefore **not launched**. No MSI/NSIS installer was run; no download/import/delete/personal-data mutation flow was run; no full e2e was run; no build command (`npm run tauri build`/`dev`) was run; no source was changed; no generated artifact was staged, committed, uploaded, installed, launched, or distributed. Process/listener state at rest: no `ytm-free` process running; no listener on 3456; port 5173 held by an unrelated pre-existing Vite dev server (node, PID 20652, project `C:\Users\gglig\my_project\D U L A P`) that was not spawned by this session and was left untouched (the release executable binds 3456, not 5173). Classification: **PASS-ARTIFACT-ONLY / BLOCKED-RUNTIME-ISOLATION** - artifacts exist and hash correctly, but release-runtime smoke remains unverified because a safe isolated data directory cannot be forced without a source change. Next minimal action (on a branch, not `main`): make `get_db_path()` honor an override env var (e.g. `YTM_FREE_DATA_DIR`) or a CLI flag so the release exe can run against a throwaway temp DB without touching real AppData, then re-run Step 3B against `src-tauri/target/release/ytm-free.exe` with `curl http://localhost:3456/health`.

**Step-3C runtime data-dir override (2026-07-07, twelfth session, branch `fix/runtime-data-dir-override`):** the isolation blocker from Step 3B is resolved at the code level. Minimal change in `src-tauri/src/db.rs::get_db_path()`: if `YTM_FREE_DATA_DIR` is set and non-empty, the SQLite DB path becomes `<YTM_FREE_DATA_DIR>\ytm-free.db` (parent dir created by the existing `create_dir_all` in `Database::new()`); when unset or empty, the default `dirs::data_dir()\ytm-free\ytm-free.db` behavior is preserved exactly. Only `db.rs` changed (+85/-1: override logic + one consolidated `test_get_db_path_override_behavior` test that sets/unsets the env var under an `EnvVarGuard` and asserts `Database::new()` creates the DB only in the override dir, never at the default AppData path; no real AppData touched, env var restored on drop). Verification (SDK-pinned `vcvarsall.bat x64 10.0.26100.0`): `cd src-tauri && cargo test` -> 29 passed, 0 failed (was 28; +1 override test); `npx tsc --noEmit` -> exit 0; `npm test` -> 32/32; `npm run build` -> built in 6.35s. The override is therefore runtime-verified at the unit-test level (a real `Database::new()` call with the override creates the DB only in the temp dir). Release-binary runtime smoke against an override-aware release exe is **BLOCKED-ENV**: the only existing release exe (`src-tauri/target/release/ytm-free.exe`, hash `9E30ED8C2E7B70F61E2D32825CD68DE03D0D17FB896BBC0124B042F339950B3C`) was built in Step 3A *before* this patch and does NOT honor `YTM_FREE_DATA_DIR`. A controlled launch was attempted with `YTM_FREE_DATA_DIR` set to an empty temp dir (`$env:TEMP\ytm-free-release-smoke-20260707174810`); the stale exe started (window title `YTM Free`, listener `127.0.0.1:3456`, `curl -i http://localhost:3456/health` -> `HTTP/1.1 200 OK` / `OK`, optional `curl -i http://localhost:3456/stream/YXZH-eBtmqQ` -> `HTTP/1.1 307 Temporary Redirect` with a `googlevideo.com/videoplayback` location, no redirect followed, no media downloaded), but it ignored the override and opened the **real** AppData DB. The real DB was opened/locked but **not mutated**: size `1482752` bytes, `LastWriteTime 2026-07-06T18:03:56`, and SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25` were identical before and after; no `-wal`/`-shm` files were created. The process was stopped, the lock released, and no listeners remained on 3456/5173. Rebuilding the release exe from the patched source is required to validate the override in the release binary, but `fsutil volume diskfree C:` showed only **2.1 GB free** (2,293,501,952 bytes) — below the >=15 GB `npm run tauri build` disk gate (RECOVERY_PLAN Step 3A / skills/repo-recovery.md #6) — so the rebuild was **not attempted** (would risk `os error 112`). No MSI/NSIS installer was run; no download/import/delete/personal-data mutation flow was run; no full e2e was run; no generated artifact was staged, committed, uploaded, installed, or distributed. Classification: **override VERIFIED (unit test + frontend gates); release-runtime re-smoke BLOCKED-ENV (disk space prevents rebuilding the release exe with the patch)**. Next minimal action (needs the human): free >=15 GB on C: (or approve `cargo clean` of `src-tauri/target`), then SDK-pinned `npm run tauri build` on this branch and re-run the controlled smoke with `YTM_FREE_DATA_DIR` -> temp dir against the rebuilt `src-tauri/target/release/ytm-free.exe` (`curl http://localhost:3456/health`, optional read-only `/stream/YXZH-eBtmqQ`).

**Step-3C re-smoke after patched release rebuild (2026-07-07, thirteenth session, branch `fix/runtime-data-dir-override`):** release-runtime isolation is now **verified for the narrow smoke** against a rebuilt override-aware executable. Preflight stayed on `fix/runtime-data-dir-override`; HEAD, local branch, and `origin/fix/runtime-data-dir-override` were all `4d898932e1c031e973d5228677060efabebfd36c`; `main` and `origin/main` remained `55fd49d62ad593de8a28265371009dc00c44e627`; only the known untracked files were present. Disk gate passed (`C:` 21.50 GB free before gates, 21.58 GB immediately before and after build; `src-tauri/target` 6.54 GB; `dist` 0.44 MB). Patched source was present in `src-tauri/src/db.rs::get_db_path()` (`YTM_FREE_DATA_DIR` non-empty -> `<dir>\ytm-free.db`; unset/empty -> default `dirs::data_dir()\ytm-free\ytm-free.db`). Baseline gates passed: `npx tsc --noEmit` exit 0; `npm test` -> 5 files / 32 tests passed; `npm run build` -> built in 3.65s; SDK-pinned `vcvarsall.bat x64 10.0.26100.0 && cd src-tauri && cargo test` -> 29 passed, 0 failed. SDK-pinned `npm run tauri build` exited 0 and rebuilt `src-tauri/target/release/ytm-free.exe` (8,761,344 bytes, SHA256 `04AABD6476FC24B4AF108E4217B0DC5FEDD75DE4B2CE952DC9E619AFE5F64A4E`) plus regenerated MSI `src-tauri/target/release/bundle/msi/YTM Free_0.1.0_x64_en-US.msi` (3,805,184 bytes, SHA256 `796C62A61EB3A557E3A04A297C1BA2D343CDC1BE17BC13856EA614A2AB59D7FB`) and NSIS `src-tauri/target/release/bundle/nsis/YTM Free_0.1.0_x64-setup.exe` (2,802,963 bytes, SHA256 `EA3F18E5ABF74454EB287A06477D9B6A70A89CE74F6707A8FE124D4E41FC5C96`). The MSI/NSIS installers were **not run**. Controlled release-runtime smoke launched only `src-tauri/target/release/ytm-free.exe` with process-local `YTM_FREE_DATA_DIR=C:\Users\gglig\AppData\Local\Temp\ytm-free-release-smoke-20260707190451`; PID 20276 showed window title `YTM Free`, responding, and listener `127.0.0.1:3456`. `curl.exe -i http://localhost:3456/health` -> `HTTP/1.1 200 OK` / `OK`; optional `curl.exe -i http://localhost:3456/stream/YXZH-eBtmqQ` -> `HTTP/1.1 307 Temporary Redirect` to a `googlevideo.com/videoplayback` URL, with no redirect followed and no media downloaded. The override temp DB was created only at `C:\Users\gglig\AppData\Local\Temp\ytm-free-release-smoke-20260707190451\ytm-free.db` (151,552 bytes, SHA256 `8159E6E72B6CA203F42C24E88AF6EA73136B22FEF160AA7477A9D3C0A600C32`). The real AppData DB `C:\Users\gglig\AppData\Roaming\ytm-free\ytm-free.db` was not mutated: before/after size `1482752`, `LastWriteTime 2026-07-06T18:03:56.7793175+03:00`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`; no real `-wal` or `-shm` files existed before or after. Cleanup stopped the release process, cleared `YTM_FREE_DATA_DIR` from the session, found no listeners on 3456 or 5173, recorded temp contents, then removed the temp smoke directory. No full e2e, download, import, delete, personal-data mutation, persistence, or restart flow was run; no generated artifact was installed, uploaded, distributed, staged, or committed.

**Step-5A NSIS installer smoke (2026-07-07, fourteenth session, main `7291d077f51640297e7c10575b506d1db2020eb2`):** NSIS installer smoke is **verified for install -> installed `/health` runtime -> uninstall cleanup**. Preflight confirmed local `main` == `origin/main` == `7291d077f51640297e7c10575b506d1db2020eb2`, `git rev-list --left-right --count origin/main...main` -> `0 0`, nothing staged, and only the known untracked `.omx` / GDPR draft files present. Inventory: NSIS `src-tauri/target/release/bundle/nsis/YTM Free_0.1.0_x64-setup.exe` existed (2,802,963 bytes, SHA256 `EA3F18E5ABF74454EB287A06477D9B6A70A89CE74F6707A8FE124D4E41FC5C96`) and was the only installer run; MSI `src-tauri/target/release/bundle/msi/YTM Free_0.1.0_x64_en-US.msi` existed for inventory only (3,805,184 bytes, SHA256 `796C62A61EB3A557E3A04A297C1BA2D343CDC1BE17BC13856EA614A2AB59D7FB`) and was **not run**. `tauri.conf.json` has `productName` `YTM Free`, version `0.1.0`, identifier `com.gabor.ytm-free`, and `bundle.targets` `all`, with no custom NSIS config; the installer was run in NSIS silent mode (`/S`) and exited 0. It installed per-user to `C:\Users\gglig\AppData\Local\YTM Free`, created `ytm-free.exe` (8,761,344 bytes, SHA256 `EBAE986F9D9B9613AE5A870503D6054B4135A0BA597C5689A8D99C4CBB1B9837`), `uninstall.exe` (79,184 bytes, SHA256 `E4542E38D88C8EF92430AD7ADF1DEB43AB073A6BAC656CA99E88261DCA180DC5`), a per-user Start Menu shortcut, and an HKCU uninstall entry. Installed runtime smoke launched only the installed `ytm-free.exe` with process-local `YTM_FREE_DATA_DIR=C:\Users\gglig\AppData\Local\Temp\ytm-free-installed-smoke-20260707212611`; PID 3588 showed window title `YTM Free`, responding, and listener `127.0.0.1:3456`; `curl.exe -i http://localhost:3456/health` -> `HTTP/1.1 200 OK` / `OK`. The temp override DB existed only at `<YTM_FREE_DATA_DIR>\ytm-free.db` (151,552 bytes, SHA256 `7D406D311B7DA955F12CFBE2D9E272E5F5C3BCD26B4006F246434DFAA7798C23`). The real AppData DB `C:\Users\gglig\AppData\Roaming\ytm-free\ytm-free.db` stayed unchanged before/after install, runtime, and uninstall: size `1482752`, `LastWriteTime 2026-07-06 18:03:56`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`; no real `-wal` or `-shm` files existed. Optional no-follow `/stream/YXZH-eBtmqQ` was attempted after the health check but the app listener was already gone, so that optional probe was not verified and no media was downloaded. Cleanup found no YTM Free process/listeners, recorded and removed the temp smoke directory, ran `C:\Users\gglig\AppData\Local\YTM Free\uninstall.exe /S` (exit 0), and verified the install directory, Start Menu shortcut, uninstall registry entry, and temp smoke directory were removed. Classification: **PASS-NSIS-INSTALLER-SMOKE**. Full e2e, download, import, delete, personal-data mutation, persistence, restart, and MSI installer behavior remain unverified. No generated artifact was staged, committed, uploaded, or distributed; no production-ready claim is made.

**Step-5B MSI installer smoke (2026-07-07, fifteenth session, main `802fc01459550ba96662038b0d19b1dbf01de592`):** MSI installer smoke is **verified for install -> installed `/health` runtime -> uninstall cleanup**. Preflight confirmed local `main` == `origin/main` == `802fc01459550ba96662038b0d19b1dbf01de592`, `git rev-list --left-right --count origin/main...main` -> `0 0`, nothing staged, and only the known untracked `.omx` / GDPR draft files present. Inventory: MSI `src-tauri/target/release/bundle/msi/YTM Free_0.1.0_x64_en-US.msi` existed (3,805,184 bytes, SHA256 `796C62A61EB3A557E3A04A297C1BA2D343CDC1BE17BC13856EA614A2AB59D7FB`) and was the only installer run; NSIS `src-tauri/target/release/bundle/nsis/YTM Free_0.1.0_x64-setup.exe` existed for inventory only (2,802,963 bytes, SHA256 `EA3F18E5ABF74454EB287A06477D9B6A70A89CE74F6707A8FE124D4E41FC5C96`) and was **not rerun**. `tauri.conf.json` has `productName` `YTM Free`, version `0.1.0`, identifier `com.gabor.ytm-free`, and `bundle.targets` `all`; MSI metadata reported `ProductCode` `{A06639C0-D8EA-439C-BE11-578042A08E66}`, `UpgradeCode` `{E84DCC00-5493-56EF-9580-66F26B4C56D3}`, `ALLUSERS=1`, and `WIXUI_INSTALLDIR=INSTALLDIR`. A first silent install invocation timed out because the space-containing MSI path was not quoted by the process command line; it produced no log, no install directory, and no uninstall entry, and the stuck `msiexec` process was stopped. The corrected quoted command `msiexec.exe /i "<msi>" /qn /norestart /L*v "<log>"` exited 0 with log `C:\Users\gglig\AppData\Local\Temp\ytm-free-msi-install-20260707220038.log`. The MSI installed to `C:\Users\gglig\AppData\Local\YTM Free`, created `ytm-free.exe` (8,761,344 bytes), a ProgramData Start Menu shortcut at `C:\ProgramData\Microsoft\Windows\Start Menu\Programs\YTM Free\YTM Free.lnk`, and an HKLM uninstall entry. Installed runtime smoke launched only the installed `ytm-free.exe` with process-local `YTM_FREE_DATA_DIR=C:\Users\gglig\AppData\Local\Temp\ytm-free-msi-installed-smoke-20260707220117`; PID 5892 showed window title `YTM Free` and listener `127.0.0.1:3456`; `curl.exe -i http://localhost:3456/health` -> `HTTP/1.1 200 OK` / `OK`. The temp override DB existed only at `<YTM_FREE_DATA_DIR>\ytm-free.db` (151,552 bytes, SHA256 `29F999A7F93643F9A19CE15E53D9D75B8ADBFD9726B0E8084E653AE117C6A7F9`). The real AppData DB `C:\Users\gglig\AppData\Roaming\ytm-free\ytm-free.db` stayed unchanged before/after install, runtime, and uninstall: size `1482752`, `LastWriteTime 2026-07-06T18:03:56.7793175+03:00`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`; no real `-wal` or `-shm` files existed. Cleanup stopped the installed app, found no YTM Free process or listener on 3456/5173 (only a transient non-listening `TIME_WAIT` entry to 3456), recorded and removed the temp smoke directory, ran `msiexec.exe /x {A06639C0-D8EA-439C-BE11-578042A08E66} /qn /norestart /L*v "<log>"` with log `C:\Users\gglig\AppData\Local\Temp\ytm-free-msi-uninstall-20260707220302.log` and exit 0, and verified the install directory, Start Menu shortcut, uninstall registry entry, and temp smoke directory were removed. Classification: **PASS-MSI-INSTALLER-SMOKE**. Full e2e, download, import, delete, personal-data mutation, persistence, and restart remain unverified. No generated artifact was staged, committed, uploaded, or distributed; no production-ready claim is made.

**Step-6A full controlled e2e preflight (2026-07-07, sixteenth session, main `7d4a93901bd9448c2d4ed6dcd2e54a86da3f4f32`):** the app surface is now **mapped well enough to plan a controlled e2e run without touching real AppData**; the e2e itself was **not run**. Preflight confirmed local `main` == `origin/main` == `7d4a93901bd9448c2d4ed6dcd2e54a86da3f4f32`, `git rev-list --left-right --count origin/main...main` -> `0 0`, nothing staged, and only the known untracked `.omx` / GDPR draft files present. Code inspection covered `src-tauri/src/lib.rs`, `db.rs`, `ytdlp.rs`, `spotify_import.rs`, `server.rs`, `src/App.tsx`, `src/api.ts`, and the search/library/import/player/playlist/settings/cleanup views. Command inventory findings: `Database::get_db_path()` still isolates all SQLite mutations under process-local `YTM_FREE_DATA_DIR`; startup also runs `cleanup_tts_files()`, which removes only temp files named `ytmfree_dj_*.mp3` from `%TEMP%`; playlist/settings/favorite/play-history/delete operations mutate only the SQLite DB; `search_youtube`, `get_stream_url`, `/health`, and the no-follow `/stream/:id` probe are read-only/network-facing; actual playback updates play history but remains temp-DB safe when launched with `YTM_FREE_DATA_DIR`; `download_track` is **not** temp-DB safe because `src-tauri/src/ytdlp.rs` writes to `dirs::audio_dir()` / `dirs::download_dir()` under `YTM-Free`. Import preflight found that `ImportView` auto-scans `get_default_spotify_folder()` on mount, but that function first resolves `C:\Users\gglig\.ytm-free\Spotify`, and this repo-local fixture folder exists with tracked CSV exports, so a controlled import phase can stay inside workspace data instead of broad user folders. Test inventory shows component/unit coverage only (`src/__tests__/*.test.ts[x]`, 32 frontend tests; 29 Rust tests across `db.rs`, `semantic.rs`, `spotify_import.rs`, `ollama/client.rs`, and `ollama/prompts.rs`); there is still **no** Playwright/Cypress/Tauri-driver runtime harness in the repo. Classification: **READY-FOR-CONTROLLED-E2E** for a future Step-6B run limited to startup + health + empty temp DB, read-only search, no-follow stream probe, import from the repo `Spotify/` fixture folder, temp-DB playlist persistence/restart, temp-DB delete, and real-AppData non-mutation proof. Full e2e was not run in this session; no installer was run; no download/import/delete/persistence/restart flow was run; download verification remains separate and unverified because the current download path escapes `YTM_FREE_DATA_DIR`. No production-ready claim is made.

**Step-6B.1-6B.3 controlled release-runtime smoke (2026-07-07, seventeenth session, main `7de91dbafcb410183e2a5140ec8c3012d4f16514`):** startup, `/health`, and no-follow stream redirect are **verified under temp `YTM_FREE_DATA_DIR` isolation**; search was **not run** because no safe automation path exists yet. Preflight confirmed local `main` == `origin/main` == `7de91dbafcb410183e2a5140ec8c3012d4f16514`, `git rev-list --left-right --count origin/main...main` -> `0 0`, nothing staged, and only the known untracked `.omx` / GDPR draft files present. Release executable inventory: `src-tauri/target/release/ytm-free.exe` existed (8,761,344 bytes, SHA256 `04AABD6476FC24B4AF108E4217B0DC5FEDD75DE4B2CE952DC9E619AFE5F64A4E`); MSI and NSIS artifacts existed for inventory only and were not run. Baseline had no `ytm-free` process and no listeners on 3456/5173. Real AppData DB before launch: `C:\Users\gglig\AppData\Roaming\ytm-free\ytm-free.db`, size `1482752`, `LastWriteTime 2026-07-06 18:03:56`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, no WAL/SHM. Controlled launch used only `src-tauri/target/release/ytm-free.exe` with process-local `YTM_FREE_DATA_DIR=C:\Users\gglig\AppData\Local\Temp\ytm-free-6b123-smoke-20260707223958`; PID 22228 showed window title `YTM Free`, responding, and listener `127.0.0.1:3456`. Temp DB was created at `<YTM_FREE_DATA_DIR>\ytm-free.db` (151,552 bytes; after process stop SHA256 `D009F8AE8C23E4727C6BD4389C6BF9B4E91FE70F8977B6AD3BCB256D3BC80A32`). `curl.exe -i http://localhost:3456/health` returned `HTTP/1.1 200 OK` / `OK`. Search was not invoked: the running app exposes only `/health`, `/stream/:id`, `/stream/video/:id`, and `/tts/:filename` over HTTP, while `search_youtube` is Tauri IPC and no Playwright/Cypress/Tauri-driver harness exists in the repo. `curl.exe -i --max-time 20 http://localhost:3456/stream/YXZH-eBtmqQ` returned `HTTP/1.1 307 Temporary Redirect` with a `googlevideo.com/videoplayback` location; `-L` was not used and no media body was downloaded. Real AppData DB after launch/probes/cleanup was unchanged by size, `LastWriteTime`, SHA256, and WAL/SHM state. Cleanup stopped PID 22228, cleared `YTM_FREE_DATA_DIR`, found no `ytm-free` process, no 5173 listener, and only transient non-listening `TIME_WAIT` entries to 3456, recorded temp contents (`ytm-free.db` only), then removed the temp directory. No installer, import, delete, download, persistence/restart, or full e2e flow was run; no generated artifact was staged, committed, uploaded, or distributed. Classification: **PASS-6B123 with SEARCH-NOT-RUN / NEEDS-HARNESS**. No production-ready claim is made.

**Step-6B.4 controlled import harness (2026-07-07, nineteenth session, branch `test/import-runtime-harness` from main `c68f77fa08126651c94a73ff548be6f6b910b146`):** the import persistence path is now **PASS-IMPORT-HARNESS** under synthetic temp input and temp `YTM_FREE_DATA_DIR` isolation, without runtime UI automation. Preflight confirmed local `main` == `origin/main` == `c68f77fa08126651c94a73ff548be6f6b910b146`, then branched to `test/import-runtime-harness`; only the known untracked `.omx` / GDPR draft files were present. The smallest safe harness was a new Rust test in `src-tauri/src/db.rs` because the real import persistence path already exists in pure Rust: parse Exportify CSV, create a playlist, upsert matched tracks, set duration, and link them via `playlist_tracks`. No release executable, installer, download, delete, persistence/restart, or full e2e flow was run. The harness used only a synthetic temp CSV with headers `Track Name, Artist Name(s), Album Name, Duration (ms), Spotify ID` and two synthetic tracks, plus a temp `YTM_FREE_DATA_DIR` for `Database::new()`. It deliberately did **not** use the repo-local `Spotify/*.csv` exports and did **not** call network search; instead it injected deterministic synthetic `ImportResult` matches to exercise the same DB mutation path the frontend uses after matching. Evidence from the targeted harness test: before import, temp DB counts were `tracks=0`, `playlists=0`, `playlist_tracks=0`; after import they were `tracks=2`, `playlists=1`, `playlist_tracks=2`; the persisted playlist reported two tracks; the persisted track durations were `123` and `245` seconds; the temp DB size was 151,552 bytes with SHA256 `5FDFE1FE50D47AFAF663A6110F9B694FDA3BE1A34AB5EF19206E2D562870BAA0`. Full SDK-pinned `cargo test -- --test-threads=1` then passed `30/30`, including the new harness. The real AppData DB `C:\Users\gglig\AppData\Roaming\ytm-free\ytm-free.db` remained unchanged before/after by size `1482752`, `LastWriteTime 2026-07-06 18:03:56.7793175+03:00`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, and no WAL/SHM files. Runtime import via Tauri IPC/UI is still not exercised directly, and search still needs its own harness; full e2e is still not complete; no production-ready claim is made.

**Step-6B.5 controlled persistence/restart smoke (2026-07-07, twentieth session, main `f12e701dfda84090014fa0e94a36e26ac7294e1d`):** baseline DB persistence across a controlled stop/relaunch is **verified under temp `YTM_FREE_DATA_DIR` isolation**; synthetic-state persistence remains **NEEDS-PERSISTENCE-HARNESS**. Preflight confirmed local `main` == `origin/main` == `f12e701dfda84090014fa0e94a36e26ac7294e1d`, `git rev-list --left-right --count origin/main...main` -> `0 0`, nothing staged, and only the known untracked `.omx` / GDPR draft files present. Release executable inventory: `src-tauri/target/release/ytm-free.exe` existed (8,761,344 bytes, SHA256 `04AABD6476FC24B4AF108E4217B0DC5FEDD75DE4B2CE952DC9E619AFE5F64A4E`); MSI and NSIS artifacts existed for inventory only and were **not run**. Baseline had no `ytm-free` process and no listeners on 3456/5173. Real AppData DB before launch: `C:\Users\gglig\AppData\Roaming\ytm-free\ytm-free.db`, size `1482752`, `LastWriteTime 2026-07-06 18:03:56.7793175+03:00`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, no WAL/SHM. Synthetic-state seeding: no safe path exists without new code or UI automation - the Step-6B.4 harness creates its own random temp dir and removes it, the running app exposes only `/health`, `/stream/:id`, `/stream/video/:id`, and `/tts/:filename` over HTTP (import and playlist mutation are Tauri IPC only), and no Playwright/Cypress/Tauri-driver harness exists; repo-local `Spotify/*.csv` personal exports were intentionally not used. Persistence was therefore tested at the baseline level: a fresh temp `YTM_FREE_DATA_DIR=C:\Users\gglig\AppData\Local\Temp\ytm-free-6b5-persistence-20260707233500` was created. Launch #1 used only `src-tauri/target/release/ytm-free.exe` with that process-local env var; PID 22604 showed window title `YTM Free`, responding, listener `127.0.0.1:3456`; `curl.exe -i http://localhost:3456/health` returned `HTTP/1.1 200 OK` / `OK`. The process was stopped; the temp DB created at `<YTM_FREE_DATA_DIR>\ytm-free.db` was 151,552 bytes, `LastWriteTime 2026-07-07T23:35:11.7677472+03:00`, SHA256 `56255904148F6BD5B67AFAB9FCFC26CDE1629C699DAD532C855DD1E3E89DF070`, no WAL/SHM. Launch #2 relaunched the same release exe with the **same** `YTM_FREE_DATA_DIR`; PID 16788 showed window title `YTM Free`, responding, listener `127.0.0.1:3456`; `curl.exe -i http://localhost:3456/health` again returned `HTTP/1.1 200 OK` / `OK`. The process was stopped and the temp DB re-inspected: size `151552`, `LastWriteTime 2026-07-07T23:35:11.7677472+03:00`, SHA256 `56255904148F6BD5B67AFAB9FCFC26CDE1629C699DAD532C855DD1E3E89DF070` - **identical** to the post-Launch-#1 state by size, `LastWriteTime`, and SHA256, with no WAL/SHM, proving the persisted baseline DB (schema from `Database::new()` + `run_migrations`) reopened unchanged on relaunch. Real AppData DB after both launches/cleanup was unchanged by size `1482752`, `LastWriteTime 2026-07-06 18:03:56.7793175+03:00`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, no WAL/SHM. Cleanup stopped both PIDs, found no `ytm-free` process and no listeners on 3456/5173, recorded temp contents (`ytm-free.db` only), then removed the temp directory. No installer, download, delete, UI import, or full e2e flow was run; no generated artifact was staged, committed, uploaded, or distributed. Classification: **PARTIAL-6B5-BASELINE-PERSISTENCE / NEEDS-PERSISTENCE-HARNESS** (baseline schema/settings persist across relaunch; synthetic imported-track/playlist persistence across restart still needs a runtime harness or a seeding path that leaves the temp DB in place). Runtime UI/Tauri IPC import and search remain unverified. No production-ready claim is made.

**Step-6B.5b persistence-state harness (2026-07-07, twenty-first session, branch `test/persistence-state-harness` from main `41a5737ce1bb1ecaa5aba45782e6be6f43628d7b`):** synthetic tracks/playlists/playlist_links are now **verified to survive a `Database::new()` reopen against the same temp `YTM_FREE_DATA_DIR`** at the Rust level, closing the NEEDS-PERSISTENCE-HARNESS gap from Step 6B.5 for the core persistence path (runtime UI/Tauri IPC still unverified). The existing Step-6B.4 import harness reopens the temp DB only via a raw `Connection::open` and checks counts, so a minimal test-only extension was added in `src-tauri/src/db.rs`: `test_controlled_persistence_state_survives_reopen` creates a fresh temp `YTM_FREE_DATA_DIR`, seeds synthetic state with `Database::new()` instance #1 (`create_playlist`, `add_track`, `update_track_duration`, `add_track_to_playlist` for two synthetic tracks `persist-video-001`/`persist-video-002` with durations 123/245s), drops instance #1, reopens the **same** temp DB with `Database::new()` instance #2 (rerunning idempotent `run_migrations` on the existing file), and asserts the state survived: counts `tracks=2, playlists=1, playlist_tracks=2` before and after reopen; the persisted playlist still reports `track_count=2` and name `Persistence Harness Playlist`; the reopened playlist tracks still match `video_id`, `title`, `artist`, and `duration` for both synthetic tracks. No CSV parsing, no network, no release exe/UI/Tauri IPC, no personal exports, and no production backdoor were used; the test uses the shared `ytm_free_data_dir_test_lock`, `EnvVarGuard`, and removes its temp dir after assertions. Focused SDK-pinned `cargo test test_controlled_persistence_state_survives_reopen -- --nocapture --test-threads=1` passed (`1 passed; 0 failed`; harness println: `before_tracks=0 before_playlists=0 before_links=0 seeded_tracks=2 seeded_playlists=1 seeded_links=2 seeded_db_size=151552 reopened_tracks=2 reopened_playlists=1 reopened_links=2 reopened_db_size=151552 reopened_db_sha256=ADE554582C32891B691CBAF064BCE538D814892CC56F330E87B1D2E43B56D114`). Full SDK-pinned `cargo test -- --test-threads=1` then passed `31/31` (was 30; +1). Real AppData DB `C:\Users\gglig\AppData\Roaming\ytm-free\ytm-free.db` was unchanged before/after by size `1482752`, `LastWriteTime 2026-07-06 18:03:56.7793175+03:00`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, no WAL/SHM. No installer, download, delete, UI import, or full e2e flow was run; no generated artifact was staged, committed, uploaded, or distributed. Classification: **PASS-PERSISTENCE-STATE-HARNESS (Rust-level)**. Runtime UI/Tauri IPC import and search remain unverified; full e2e is still not complete. No production-ready claim is made.

**Step-6B.6 controlled delete-state harness (2026-07-08, branch `test/delete-state-harness` from main `e4a5df46ecd3dfa28da988ed67f6253cebdc50da`):** playlist-link removal is now **verified to persist after a `Database::new()` reopen against the same temp `YTM_FREE_DATA_DIR`** at the Rust level. Inspection found the narrowest existing delete path is `Database::remove_track_from_playlist`, exposed as the `remove_from_playlist` Tauri command; it deletes only the matching `playlist_tracks` row. Broader existing delete paths are `delete_playlist` (deletes from `playlists`; `playlist_tracks.playlist_id` has `ON DELETE CASCADE`) and `delete_track` (explicitly deletes from `track_metadata`, `playlist_tracks`, `play_history`, then `tracks`; other track-linked tables also declare `ON DELETE CASCADE`). The new test-only harness `test_controlled_delete_state_persists_after_reopen` creates a fresh temp data dir, sets `YTM_FREE_DATA_DIR`, seeds two synthetic tracks (`delete-video-001`/`delete-video-002`, durations 123/245s), one synthetic playlist, and two playlist links via existing DB methods only, removes `delete-video-001` from the playlist via `remove_track_from_playlist`, and verifies tracks/playlists remain while `playlist_tracks` drops from 2 to 1. It then drops the first `Database`, reopens the same temp DB with `Database::new()`, and verifies the removal persisted: `tracks=2`, `playlists=1`, `playlist_tracks=1`, removed link count `0`, kept link count `1`, playlist `track_count=1`, and only `delete-video-002` remains in `get_playlist_tracks`. Focused SDK-pinned `cargo test test_controlled_delete_state_persists_after_reopen -- --nocapture --test-threads=1` passed (`1 passed; 0 failed`; harness println included `before_tracks=0 before_playlists=0 before_links=0 seeded_tracks=2 seeded_playlists=1 seeded_links=2 after_delete_tracks=2 after_delete_playlists=1 after_delete_links=1 after_delete_db_size=151552 reopened_tracks=2 reopened_playlists=1 reopened_links=1 removed_link_count=0 kept_link_count=1 reopened_db_size=151552 reopened_db_sha256=1D5B8547B1696A0A3411D2C3CD6DD096994AABA35A3AE84DBAA6EE08AC1FE3B7`). Full SDK-pinned `cargo test -- --test-threads=1` passed `32/32` (was 31; +1). The real AppData DB `C:\Users\gglig\AppData\Roaming\ytm-free\ytm-free.db` was unchanged before/after by size `1482752`, `LastWriteTime 2026-07-06T18:03:56.7793175+03:00`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, and no WAL/SHM. No personal Spotify CSV exports, installer, runtime UI, download, UI import, or full e2e flow was run; no generated artifact was staged, committed, uploaded, or distributed. Runtime UI/Tauri IPC delete remains unverified. No production-ready claim is made.

**Step-6B.7 controlled search-state harness (2026-07-08, branch `test/search-state-harness` from main `b100e9b6ee3408d04da0d45be525f91fe34630cd`):** Rust-level local DB metadata filter/search state is now **verified to persist after a `Database::new()` reopen against the same temp `YTM_FREE_DATA_DIR`**. Discovery found the user-facing `search_youtube` path is a Tauri IPC command that calls `yt-dlp`/network, Spotify smart search also calls external search/Ollama paths, and semantic search requires Tauri state plus Ollama embeddings, so those runtime/external search paths were not selected. The smallest safe local path was the existing DB metadata filters: `get_tracks_by_genre`, `get_tracks_by_mood`, and `get_tracks_by_energy_range`. The new test-only harness `test_controlled_search_state_filters_persist_after_reopen` creates a fresh temp data dir, sets `YTM_FREE_DATA_DIR`, seeds three synthetic tracks and three synthetic metadata rows via existing DB methods only, verifies exact genre and mood matches, an absent exact genre miss, and inclusive energy-range ordering by descending energy, then drops and reopens the same temp DB with `Database::new()` and verifies the same results. Focused SDK-pinned `cargo test test_controlled_search_state_filters_persist_after_reopen -- --nocapture --test-threads=1` passed (`1 passed; 0 failed`; harness println included `seeded_tracks=3 seeded_metadata=3 synthwave_matches=2 focus_matches=1 high_energy_matches=2 missing_genre_matches=0 reopened_tracks=3 reopened_metadata=3 reopened_synthwave_matches=2 reopened_focus_matches=1 reopened_high_energy_matches=2 reopened_missing_genre_matches=0 reopened_db_size=151552 reopened_db_sha256=F996C97F04DD7A292757970C0C4274E274098152312DD54B197F5F23E87E4127`). Full SDK-pinned `cargo test -- --test-threads=1` passed `33/33` (was 32; +1). `cargo fmt --check` was run read-only and failed because the existing tree is not rustfmt-clean and has pre-existing trailing whitespace in `src-tauri/src/db.rs`; no format rewrite was applied. The real AppData DB `C:\Users\gglig\AppData\Roaming\ytm-free\ytm-free.db` was unchanged before/after by size `1482752`, `LastWriteTime 2026-07-06T18:03:56.7793175+03:00`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, and no WAL/SHM. No personal Spotify CSV exports, installer, runtime UI, Tauri IPC search, download, UI import, delete runtime flow, or full e2e flow was run; no generated artifact was staged, committed, uploaded, or distributed. Runtime UI/Tauri IPC search remains unverified. No production-ready claim is made.

**Step-6D.1 stateful IPC DB helper harness (2026-07-08, branch `test/stateful-ipc-db-harness` from main `c10df0f11a43aa4acc69f92e85b14770732c5600`):** the playlist IPC DB mutation path is now **verified at the Rust level through extracted DB-only helpers** that the public Tauri wrappers delegate to, closing the gap between the Tauri `State<AppState>`-bound wrappers and the pure-DB paths exercised by prior harnesses. The change in `src-tauri/src/lib.rs` extracts five DB-only helper functions that take only a `&Database` and plain arguments (no `State`, no `AppHandle`, no Tauri runtime): `create_playlist_db_helper`, `add_to_playlist_db_helper`, `remove_from_playlist_db_helper`, `delete_playlist_db_helper`, and `cleanup_delete_track_db_helper`. The public Tauri IPC wrappers (`create_playlist`, `add_to_playlist`, `remove_from_playlist`, `delete_playlist`, `cleanup_delete_track`) remain thin `State<'_, AppState>` adapters that lock the DB and delegate to the helpers, preserving their signatures exactly. The new test-only harness `test_controlled_stateful_ipc_db_playlist_wrappers_with_temp_database` creates a fresh temp `YTM_FREE_DATA_DIR`, sets the env var under the shared `EnvVarGuard` + `ytm_free_data_dir_test_lock`, opens a real `Database::new()` in the temp dir, and exercises all five helpers: creates a synthetic playlist (`Stateful IPC Harness Playlist`), adds two synthetic tracks (`stateful-ipc-video-001`/`stateful-ipc-video-002`, durations 123/245s), verifies `get_playlist_tracks` returns 2 with correct video IDs/durations, removes track A and verifies 1 remains (track B only, track A still queryable by UUID), saves synthetic `TrackMetadataAI` for track B, creates synthetic play history, deletes the playlist via the helper, observes `delete_playlist_link_count=0` in the temp DB (FK cascade behavior was **observed**, not assumed — the test asserts `playlist_tracks_after_delete.len() <= 1` and records the observed count), cleans up track B via the helper and verifies track/metadata/play-history/playlist-link removal, then drops the DB, reopens the same temp DB with a second `Database::new()`, and verifies the reopened state (playlist gone, track A present, track B gone, metadata gone, play history empty, playlist links empty). The test uses no `State<AppState>`, no `tauri::Builder`, no `AppHandle`, no UI, no network, no `yt-dlp`, no Ollama, no download, no real AppData, and no personal CSV data. Focused test (SDK-pinned via the profile-loaded PowerShell shell, no manual `vcvarsall`): `cargo test test_controlled_stateful_ipc_db_playlist_wrappers_with_temp_database -- --nocapture --test-threads=1` → `1 passed; 0 failed`; harness println: `STATEFUL_IPC_DB_HARNESS playlist_id=... track_a=... track_b=... delete_playlist_link_count=0 temp_dir=...`. Full Rust gate: `cargo test -- --test-threads=1` → **35 passed / 0 failed** (was 34; +1). `cargo test --no-run` → exit 0. `cargo fmt --check` → **PARTIAL-FMT-PREEXISTING**: the existing tree is not rustfmt-clean (pre-existing trailing whitespace in `db.rs`, plus formatting debt in `spotify_import.rs`, `ytdlp.rs`, `server.rs`, `semantic.rs`); the new Step-6D.1 hunk in `lib.rs` does **not** appear in the fmt output and none of the Step-6D.1 markers (`test_controlled_stateful_ipc_db_playlist_wrappers_with_temp_database`, `STATEFUL_IPC_DB_HARNESS`, `Stateful IPC Harness Playlist`, `stateful-ipc-video-001`, `stateful-ipc-video-002`) appear in the fmt diff. Real AppData DB `C:\Users\gglig\AppData\Roaming\ytm-free\ytm-free.db` remained **unchanged** before/after by size `1482752`, `LastWriteTime 2026-07-06T18:03:56.7793175+03:00`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, WAL/SHM absent; Local AppData DB not found before/after. Temp cleanup PASS: no `ytm-free-stateful-ipc-db-harness-*` directories remained in `%TEMP%` after the tests. Runtime UI/Tauri IPC import/search/delete were **NOT RUN**; Tauri runtime **NOT RUN**; download flow **NOT RUN**; full e2e **NOT RUN**; installer **NOT RUN**; release build **NOT RUN**; production-ready **NO**.

**Step-6E.1 AI/semantic DB-cache helper harness (2026-07-08, branch `test/ai-db-cache-ipc-harness` from main `bc847be5a150d0207757df8ca486a0d84b613137`):** the safe AI/semantic DB-cache/status subset is now **verified at the Rust level through extracted DB-only helpers** that the public Tauri wrappers delegate to. The change in `src-tauri/src/lib.rs` extracts four DB-only helper functions that take only `&Database` and plain arguments: `ollama_get_track_metadata_db_helper`, `ollama_get_untagged_count_db_helper`, `get_semantic_status_db_helper`, and `semantic_clear_index_db_helper`. The public Tauri IPC wrappers (`ollama_get_track_metadata`, `ollama_get_untagged_count`, `get_semantic_status`, `semantic_clear_index`) remain thin `State<'_, AppState>` adapters that lock the DB and delegate to the helpers, preserving their signatures exactly. The new test-only harness `test_controlled_ai_semantic_db_cache_helpers_with_temp_database` creates a fresh temp `YTM_FREE_DATA_DIR`, opens a real `Database::new()` in the temp dir, seeds two synthetic tracks (`ai-cache-video-001`/`ai-cache-video-002`), synthetic `TrackMetadataAI` for track A, and synthetic local embeddings for both tracks. It verifies metadata lookup returns track A metadata and no track B metadata, untagged count is `1`, semantic status before clear is `total_tracks=2` / `indexed_tracks=2`, `semantic_clear_index` removes only embeddings, semantic status after clear is `total_tracks=2` / `indexed_tracks=0`, tracks and non-semantic metadata remain, then drops and reopens the same temp DB with `Database::new()` and verifies the final state persists. Gates (profile-loaded PowerShell shell): `cargo test --no-run` exit 0; focused `cargo test test_controlled_ai_semantic_db_cache_helpers_with_temp_database -- --nocapture --test-threads=1` -> `1 passed; 0 failed`; harness println: `AI_SEMANTIC_DB_CACHE_HARNESS tracks=2 untagged_count=1 semantic_before=2 semantic_after=0 temp_dir=...`. Full Rust gate: `cargo test -- --test-threads=1` -> **36 passed / 0 failed** (was 35; +1). `cargo fmt --check` remains **PARTIAL-FMT-PREEXISTING**: the existing tree is not rustfmt-clean, and the new Step-6E.1 markers do **not** appear in the rustfmt output. Real AppData stayed unchanged: Roaming DB size `1482752`, `LastWriteTime 2026-07-06T18:03:56.7793175+03:00`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, WAL/SHM absent; Local DB not found before/after. Temp cleanup PASS: no `ytm-free-ai-semantic-db-cache-harness-*` directories remained. Ollama real **NOT RUN**; `yt-dlp` **NOT RUN**; network **NOT RUN**; runtime UI/Tauri IPC import/search/delete **NOT RUN**; Tauri runtime **NOT RUN**; download flow **NOT RUN**; full e2e **NOT RUN**; installer **NOT RUN**; release build **NOT RUN**; production-ready **NO**.

**Step-6F.1 controlled semantic search stub helper harness (2026-07-08, branch `test/semantic-search-stub-harness` from main `9bdaace150e4415dd4dc6364399739d03d81cc8f`):** the semantic search DB/scoring core is now **verified at the Rust level with a precomputed synthetic query embedding**. The change in `src-tauri/src/lib.rs` extracts `semantic_search_with_embedding_db_helper`, which takes only `&Database`, a query embedding slice, and the existing limit value, then reads `track_embeddings`, computes local cosine similarity, applies the existing `> 0.3` threshold, sorts by descending similarity, truncates to the requested limit, and builds `SemanticSearchResult` rows from DB tracks. The public `semantic_search(State<AppState>, query, limit)` IPC wrapper keeps its signature and still performs the runtime Ollama embedding step before delegating to the helper; runtime IPC semantic search itself was **NOT RUN**. The new test-only harness `test_controlled_semantic_search_stub_uses_temp_db_embeddings` creates a fresh temp `YTM_FREE_DATA_DIR`, opens a real `Database::new()` in the temp dir, seeds three synthetic embedded tracks (`semantic-stub-video-001`/`002`/`003`, titles `Semantic Stub Song Near`/`Medium`/`Far`) plus one synthetic track without an embedding, inserts deterministic local embeddings, calls only the DB helper with `query_embedding=[1.0, 0.0, 0.0]`, verifies deterministic ordering (`001` before `002` before `003`), limit behavior (`limit=2` returns 2), non-embedded tracks are absent, then reopens the same temp DB with `Database::new()` and verifies the same ordering persists. Gates (profile-loaded PowerShell shell): `cargo test --no-run` exit 0 after one initial 120s timeout/retry; focused `cargo test test_controlled_semantic_search_stub_uses_temp_db_embeddings -- --nocapture --test-threads=1` -> `1 passed; 0 failed`; harness println: `SEMANTIC_SEARCH_STUB_HARNESS results=3 top=semantic-stub-video-001 limit=2 temp_dir=...`. Full Rust gate: `cargo test -- --test-threads=1` -> **37 passed / 0 failed** (was 36; +1). `cargo fmt --check` remains **PARTIAL-FMT-PREEXISTING**: existing global rustfmt debt remains, and the new Step-6F.1 markers do **not** appear in rustfmt output after manual hunk formatting. Real AppData stayed unchanged: Roaming DB size `1482752`, `LastWriteTime 2026-07-06T15:03:56.7793175Z`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, WAL/SHM absent; Local DB not found before/after. Temp cleanup PASS: no `ytm-free-semantic-search-stub-harness-*` directories remained. Ollama real **NOT RUN**; `yt-dlp` **NOT RUN**; network **NOT RUN**; process-spawn download/search paths **NOT RUN**; runtime UI/Tauri IPC import/search/delete **NOT RUN**; semantic search runtime IPC **NOT RUN**; Tauri runtime **NOT RUN**; download flow **NOT RUN**; full e2e **NOT RUN**; installer/release **NOT RUN**; production-ready **NO**.
**Step-6G.1 controlled semantic filtered search stub helper harness (2026-07-09, branch `test/semantic-filtered-stub-harness` from main `a7aca58d01a138e0cfe6fb08e2bffb6670bdda50`):** semantic filtered-search DB fallback/scoring is **verified at Rust level with a precomputed synthetic query embedding**. `src-tauri/src/lib.rs` now has `semantic_search_filtered_with_embedding_db_helper`, taking `&Database`, a query embedding, limit, and the existing genre/mood/activity filter arguments. It preserves the observed DB fallback behavior for genre and mood filtering, ordering, limit handling, and result construction; activities and min_similarity are **NOT VERIFIED** because the observed DB fallback does not apply them. The public `semantic_search_filtered` IPC signature is unchanged and runtime IPC was **NOT RUN**. Harness `test_controlled_semantic_filtered_search_stub_uses_temp_db_embeddings` uses a temp `YTM_FREE_DATA_DIR`, `Database::new()`, four synthetic tracks (`semantic-filtered-video-001`/`002`/`003`/`004`), synthetic metadata, deterministic embeddings, and `query_embedding=[1.0, 0.0, 0.0]`; it verifies unfiltered order, genre=`rock`, mood=`happy`, combined genre+mood, limit behavior, non-embedded track exclusion, and persistence after reopen. Gates: `cargo test --no-run` PASS; focused test PASS (`1 passed; 0 failed`); full Rust gate PASS (`38 passed; 0 failed`). `cargo fmt --check` remains **PARTIAL-FMT-PREEXISTING** and Step-6G.1 markers do not appear in rustfmt output after manual hunk formatting. Real AppData unchanged before/after; temp cleanup PASS. Ollama real **NOT RUN**; `yt-dlp` **NOT RUN**; network **NOT RUN**; runtime UI/Tauri IPC import/search/delete **NOT RUN**; semantic search runtime IPC **NOT RUN**; semantic filtered runtime IPC **NOT RUN**; Tauri runtime **NOT RUN**; download/full e2e/installer/release **NOT RUN**; production-ready **NO**.

**Step-6I semantic playlist save stub helper harness (2026-07-09, branch `test/semantic-playlist-save-stub-harness` from main `5686cbf09b9b644da1082d278c2fa4a0d55a71b1`):** semantic playlist creation/save is **verified at Rust level with precomputed synthetic scored results** in a temp DB. `src-tauri/src/lib.rs` now has `create_semantic_playlist_from_scored_results_db_helper`, taking `&Database`, `&str` query, `Option<String>` playlist_name, and `Vec<SemanticSearchResult>` — no `State<AppState>`, no `AppHandle`, no `OllamaClient`, no `embed_single`, no network. The public `create_semantic_playlist(State<AppState>, query, playlist_name)` IPC signature is unchanged; the wrapper still does runtime embedding/Ollama scoring, then resolves scored IDs to `SemanticSearchResult` rows and delegates playlist creation to the helper. Runtime IPC was **NOT RUN**. Harness `test_controlled_semantic_playlist_save_stub_uses_temp_db_results` uses a temp `YTM_FREE_DATA_DIR`, `Database::new()`, three synthetic tracks (`semantic-playlist-video-001`/`002`/`003`, titles `Semantic Playlist Song Alpha`/`Beta`/`Gamma`), precomputed scored results (0.91/0.78/0.44), explicit `playlist_name="Semantic Playlist Stub"`; it verifies playlist name, `track_count=3`, `average_similarity≈0.71`, track ordering (001 > 002 > 003), and persistence after `Database::new()` reopen. Gates: `cargo test --no-run` PASS (47.20s); focused test PASS (`1 passed; 0 failed`; harness println: `SEMANTIC_PLAYLIST_STUB_HARNESS playlist_id=... tracks=3 top=semantic-playlist-video-001 avg=0.71`); full Rust gate PASS (`39 passed; 0 failed`). `cargo fmt --check` remains **PARTIAL-FMT-PREEXISTING** — Step-6I markers do not appear in rustfmt output. Real AppData unchanged before/after; temp cleanup PASS. Ollama real **NOT RUN**; `yt-dlp` **NOT RUN**; network **NOT RUN**; runtime UI/Tauri IPC import/search/delete **NOT RUN**; semantic search runtime IPC **NOT RUN**; semantic filtered runtime IPC **NOT RUN**; `create_semantic_playlist` runtime IPC **NOT RUN**; Tauri runtime **NOT RUN**; download/full e2e/installer/release **NOT RUN**; production-ready **NO**.
**Step-6J.1 controlled semantic index-track stub helper harness (2026-07-09, branch `test/semantic-index-track-stub-harness` from main `50aa58a89ab0c98656f3700739e6b0b61c76727f`):** the safe post-embedding part of `semantic_index_track` is now **verified at Rust level with a precomputed synthetic embedding** in a temp DB. `src-tauri/src/lib.rs` now has `semantic_index_track_with_embedding_db_helper`, which takes `&Database`, a track UUID, an embedding slice, and a model name; it rebuilds `text_used` from the existing track plus metadata via `build_track_text`, saves the provided embedding with existing DB methods, and returns the persisted `TrackEmbedding`. The public `semantic_index_track(State<AppState>, track_id)` IPC signature is unchanged; the wrapper still performs runtime Ollama embedding first, then delegates the DB save step to the helper. Runtime IPC `semantic_index_track` was **NOT RUN**. Harness `test_controlled_semantic_index_track_stub_saves_temp_db_embedding` uses a temp `YTM_FREE_DATA_DIR`, `Database::new()`, one synthetic track (`semantic-index-video-001`, title `Semantic Index Song One`, artist `Semantic Index Artist One`), synthetic metadata (`semantic-index-genre-rock`, `semantic-index-mood-focused`, `semantic-index-activity-coding`, keywords `semantic/index/stub/harness`), precomputed embedding `[1.0, 0.0, 0.0]`, and model `semantic-index-stub-model`; it verifies embedding persistence, dimensions `3`, model, `text_used` content, `count_embeddings=1`, and persistence after `Database::new()` reopen. Gates: `cargo test --no-run` PASS; focused test PASS (`1 passed; 0 failed`; harness println: `SEMANTIC_INDEX_TRACK_STUB_HARNESS video=semantic-index-video-001 dimensions=3 embeddings=1`); full Rust gate PASS (`40 passed; 0 failed`). `cargo fmt --check` remains **PARTIAL-FMT-PREEXISTING** — Step-6J.1 markers do not appear in rustfmt output after manual hunk formatting. Real AppData unchanged before/after; temp cleanup PASS. Ollama real **NOT RUN**; `embed_single` runtime path **NOT RUN**; `semantic_index_track` runtime IPC **NOT RUN**; `semantic_index_all` **NOT RUN / DEFERRED**; `yt-dlp` **NOT RUN**; network **NOT RUN**; runtime UI/Tauri IPC import/search/delete **NOT RUN**; semantic search/filter/playlist runtime IPC **NOT RUN**; Tauri runtime **NOT RUN**; download/full e2e/installer/release **NOT RUN**; production-ready **NO**.
**Step-6K.1 controlled smart playlist match-library DB helper harness (2026-07-09, branch `test/smart-playlist-match-library-harness` from main `741b6af028d0dd3f306f5d180988031b4b6c3ade`):** the metadata-only matching core behind `smart_playlist_match_library` is now verified at Rust level through `smart_playlist_match_library_db_helper`, a read-only DB helper that takes `&Database`, existing genre/mood/energy/decade/activity filters, and limit, then applies the existing partial-score matching over track metadata. The public `smart_playlist_match_library(State<AppState>, genres, moods, energy_min, energy_max, decades, activities, limit)` IPC signature is unchanged; the wrapper only locks DB state and delegates to the helper. Runtime IPC `smart_playlist_match_library` was **NOT RUN**. Harness `test_controlled_smart_playlist_match_library_filters_temp_db_metadata` uses a temp `YTM_FREE_DATA_DIR`, `Database::new()`, five synthetic tracks (`smart-playlist-video-001` through `smart-playlist-video-005`) and synthetic metadata for genre, mood, energy_level, decade, and activity_tags. It verifies genre=`rock`, mood=`focus`, activity=`coding`, high-energy range, decade=`2020s`, combined partial-score behavior with `smart-playlist-video-001` ranked first, limit behavior, no-match exclusion, and persistence after `Database::new()` reopen. `year` is **NOT APPLICABLE** because the current metadata schema exposes decade, not a separate year field. Gates: `cargo test --no-run` PASS; focused test PASS (`1 passed; 0 failed`; harness println: `SMART_PLAYLIST_MATCH_LIBRARY_HARNESS ...`); full Rust gate PASS (`41 passed; 0 failed`). `cargo fmt --check` remains **PARTIAL-FMT-PREEXISTING**; Step-6K.1 markers do not appear in rustfmt output. Real AppData unchanged before/after; temp cleanup PASS. Smart playlist save/mutation **NOT RUN**; Ollama real **NOT RUN**; `yt-dlp` **NOT RUN**; network **NOT RUN**; `semantic_index_all` **NOT RUN / DEFERRED**; min_similarity semantic behavior **NOT VERIFIED**; runtime UI/Tauri IPC import/search/delete **NOT RUN**; Tauri runtime **NOT RUN**; download/full e2e/installer/release **NOT RUN**; production-ready **NO**.
**Step-6L.1 semantic filtered parity: activities + min_similarity (2026-07-09, branch `fix/semantic-filtered-parity` from main `85f3645071ff680ac0a97779a32774b132e1703a`):** the semantic filtered-search parity gap (activities ignored, `min_similarity` ignored) is **closed at the Rust level** with deterministic synthetic data; no commit was made. Two surgical changes: (1) `src-tauri/src/semantic.rs` `ANNIndex::search_filtered` now applies `filter.min_similarity` — when `Some(x)`, results with similarity strictly below `x` are excluded via `results.retain(|(_, sim)| (*sim as f64) >= threshold)`; when `None`, the existing behavior (no threshold filtering) is preserved, and the existing genre/mood/activity filtering is unchanged; (2) `src-tauri/src/lib.rs` `semantic_search_filtered_with_embedding_db_helper` no longer ignores `activities` (`let _ = activities;` removed) — it now parses `TrackMetadataDB.activity_tags` (a JSON-array `Option<String>`) via `crate::semantic::parse_json_array` and retains tracks whose tags contain any requested activity, mirroring the existing genre/mood retain convention (missing metadata/field keeps the track); a new `min_similarity: Option<f64>` parameter was added to this internal helper-only function and applied with the same `>= threshold` semantics, and the `semantic_search_filtered` wrapper call site now forwards `filter.min_similarity` (i.e. `Some(0.3)`). The public `semantic_search_filtered` IPC signature is unchanged; the threshold constant stays `Some(0.3)` from the wrapper, matching the existing non-filtered helper's `> 0.3` threshold intent (boundary note: the filtered path keeps `sim >= threshold`, the older non-filtered helper uses strict `> 0.3`; synthetic tests avoid the exact 0.3 boundary). Frontend/TypeScript API was **NOT** changed. Two new tests: `semantic::tests::test_filtered_search_respects_min_similarity` (ANN path: unit-norm synthetic embeddings with sims 1.0/0.9/0.2; verifies `min_similarity=Some(0.3)` excludes the 0.2 track, `None` keeps all three, and activities+threshold isolates the single over-threshold coding track) and `tests::test_controlled_semantic_filtered_search_stub_respects_activities_and_min_similarity` (DB helper path: temp `YTM_FREE_DATA_DIR`, `Database::new()`, three synthetic tracks `semantic-parity-video-001`/`002`/`003` with `over_match` sim 1.0 + `coding`, `over_mismatch` sim 0.9 + `driving`, `sub_match` sim 0.2 + `coding`; query `[1.0,0.0,0.0]`; verifies threshold-only excludes `sub_match`, activities-only excludes `over_mismatch`, combined isolates `over_match`, and no-filter keeps deterministic descending order). The existing `test_controlled_semantic_filtered_search_stub_uses_temp_db_embeddings` was extended to forward `Some(0.3)` at all six call sites (all existing sims ≥ 0.4, so prior assertions still hold) and its outdated comment was updated. Gates (bash shell already bootstrapped to SDK `10.0.26100.0`, `dbghelp.lib` present): `cargo test --no-run` PASS (Finished 56.19s, only pre-existing dead-code warnings); focused `cargo test test_controlled_semantic_filtered_search_stub_uses_temp_db_embeddings` PASS; focused `test_controlled_semantic_filtered_search_stub_respects_activities_and_min_similarity` PASS (harness println: `SEMANTIC_FILTERED_PARITY_HARNESS results=3 activity=2 threshold=2 top=semantic-parity-video-001`); focused `test_filtered_search_respects_min_similarity` PASS; full Rust gate `cargo test -- --test-threads=1` -> **43 passed / 0 failed** (was 41; +2 new tests). `git diff --check` clean; no CR-at-EOL in the changed files; `cargo fmt --check` -> **PASS-FMT-CLEAN** (exit 0) — the rustfmt debt noted as PARTIAL-FMT-PREEXISTING in earlier steps was closed by the prior `chore: apply rustfmt formatting` commit on main, and the new 6L.1 hunks were formatted via `cargo fmt`. Real AppData unchanged before/after: Roaming DB size `1482752`, mtime epoch `1783350236`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, WAL/SHM absent; Local DB not found before/after. Temp check (read-only, nothing deleted): no `ytm-free-semantic-filtered-parity-*` dirs remained (the parity test self-cleans); many pre-existing `ytm-free-db-override-test-*` dirs from other suite tests were present and left untouched. Runtime IPC `semantic_search_filtered` **NOT RUN**; UI min_similarity control **NOT IMPLEMENTED / NOT VALIDATED**; Tauri runtime **NOT RUN**; Ollama real **NOT RUN**; `yt-dlp` **NOT RUN**; network **NOT RUN** (except `git fetch origin`); `semantic_index_all` **NOT RUN / DEFERRED**; AppData real **NOT MUTATED**; pre-existing temp dirs matching ytm-free-db-override-test-* **NOT manually deleted** (focused harnesses may auto-clean their own temp directories); download/full e2e/installer/release **NOT RUN**; no commit/push/merge/rebase/amend/tag; production-ready **NO**.
**Step-6M.1 semantic index-track lock-scope split (2026-07-09, branch `fix/semantic-index-track-lock-scope` from main `10ca2c48251b0954724d177132c7bb9994cbc409`):** `semantic_index_track` no longer holds `state.db.lock().await` across `OllamaClient::embed_single(...).await`. `src-tauri/src/lib.rs` now adds an internal pre-embedding DB-only stage, `semantic_index_track_prepare_embedding_input_db_helper`, plus `SemanticIndexTrackPrepareInput`; the helper reads settings, track, and metadata from a real `Database`, enforces `semantic_search_enabled`, builds the embedding text via the existing `build_track_text`, and returns `track_id`, `video_id`, `embedding_text`, `ollama_url`, and `embedding_model` without calling Ollama and without writing embeddings. The existing post-embedding helper `semantic_index_track_with_embedding_db_helper` remains the save stage and its focused harness stays intact. New focused harness `test_controlled_semantic_index_track_prepare_embedding_input_uses_temp_db_metadata` uses a fresh temp `YTM_FREE_DATA_DIR`, a synthetic track `semantic-index-lock-scope-video-001`, and synthetic metadata; it verifies the prepare helper returns the expected ID/model/text, keeps `count_embeddings=0` before save, then saves a synthetic `[1.0, 0.0, 0.0]` embedding through the existing post-helper (harness print: `SEMANTIC_INDEX_TRACK_LOCK_SCOPE_HARNESS video=semantic-index-lock-scope-video-001 text_len=281 embeddings_before=0 embeddings_after=1`). Gates: `git diff --check` PASS; `cargo fmt --check` PASS after one minimal test-call formatting fix; toolchain profile autoload remains **PARTIAL** (vars still unset after `. $PROFILE.CurrentUserAllHosts`), but manual bootstrap to Windows SDK `10.0.26100.0` succeeded; `cargo test --no-run` PASS; focused existing semantic index-track stub PASS; focused new prepare-input harness PASS; full Rust gate PASS `44 passed / 0 failed`. Real AppData remained unchanged before/after (Roaming DB size `1482752`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, no WAL/SHM; Local DB absent both times). Runtime IPC `semantic_index_track` **NOT RUN**; Ollama real **NOT RUN**; Tauri runtime/UI **NOT RUN**; `semantic_index_all` **NOT RUN / DEFERRED**; temp dirs were **NOT manually deleted** (new harness dirs auto-cleaned; pre-existing `ytm-free-db-override-test-*` dirs left untouched). No production-ready claim is made.
**Step-6M.3 controlled semantic search lock-scope split (2026-07-09, branch `fix/semantic-search-lock-scope` from main `44975f9dc5585be690a60f07f4b2b490680022b7`):** `semantic_search` no longer holds `state.db.lock().await` across `OllamaClient::embed_single(...).await`. `src-tauri/src/lib.rs` now adds a pre-embedding DB-only stage, `semantic_search_prepare_embedding_input_db_helper`, plus `SemanticSearchPrepareInput`; the helper reads settings from a real `Database`, enforces `semantic_search_enabled`, and returns the query together with `ollama_url` and `embedding_model` without calling Ollama and without writing embeddings. The existing post-embedding helper `semantic_search_with_embedding_db_helper` remains the scoring/readback stage. New focused harness `test_controlled_semantic_search_prepare_embedding_input_uses_temp_db_settings` uses temp `YTM_FREE_DATA_DIR`, synthetic settings (`semantic_search_enabled=true`, synthetic Ollama URL/model), synthetic query `semantic-search-lock-scope-query`, and one synthetic saved embedding to verify the prepare stage returns the expected query/model/URL, leaves `count_embeddings=0` before save, and preserves post-helper behavior (`SEMANTIC_SEARCH_LOCK_SCOPE_HARNESS query=semantic-search-lock-scope-query model=semantic-search-lock-scope-model results=1 ...`). The existing focused harness `test_controlled_semantic_search_stub_uses_temp_db_embeddings` still passes unchanged. Gates: `git diff --check` PASS; no CR-at-EOL in touched files; `cargo fmt --check` PASS; `. $PROFILE.CurrentUserAllHosts` loaded a valid MSVC/Windows SDK shell (`WindowsSDKVersion=10.0.26100.0`, `LIB` contains `10.0.26100.0`, `dbghelp.lib` present); `cargo test --no-run` PASS; focused existing PASS; focused new PASS; full Rust gate PASS `45 passed / 0 failed`. Real AppData remained unchanged before/after (Roaming DB size `1482752`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, no WAL/SHM; Local DB absent both times). Active UI caller remains `SearchView`, but runtime IPC `semantic_search` **NOT RUN**; Ollama real **NOT RUN**; Tauri runtime/UI **NOT RUN**; `semantic_search_filtered` **NOT RUN / DEFERRED**; `create_semantic_playlist` **NOT RUN / DEFERRED**; `semantic_index_all` **NOT RUN / DEFERRED**; temp dirs were **NOT manually deleted** (new harness dirs auto-cleaned; pre-existing `ytm-free-db-override-test-*` dirs left untouched). No production-ready claim is made.
**Step-6M.5 controlled semantic filtered search lock-scope split (2026-07-09, branch `fix/semantic-filtered-search-lock-scope` from main `755d1c7263c09a5fa3971fa8fcff25cb84029ebb`):** `semantic_search_filtered` no longer holds `state.db.lock().await` across `OllamaClient::embed_single(...).await`. `src-tauri/src/lib.rs` now adds a pre-embedding DB-only stage, `semantic_search_filtered_prepare_embedding_input_db_helper`, plus `SemanticSearchFilteredPrepareInput`; the helper reads settings from a real `Database`, enforces `semantic_search_enabled`, preserves the existing internal `min_similarity=Some(0.3)` threshold and filter payload (`genres` / `moods` / `activities`), and returns the query together with `ollama_url`, `embedding_model`, and the current `SemanticSearchFilter` without calling Ollama and without writing embeddings. The existing post-embedding helper `semantic_search_filtered_with_embedding_db_helper` remains the DB fallback/scoring stage, and the public `semantic_search_filtered` IPC signature is unchanged. New focused harness `test_controlled_semantic_filtered_search_prepare_embedding_input_uses_temp_db_settings` uses temp `YTM_FREE_DATA_DIR`, synthetic settings (`semantic_search_enabled=true`, synthetic Ollama URL/model), synthetic query `semantic-filtered-lock-scope-query`, synthetic filters (`rock` / `focus` / `coding`), and one synthetic saved embedding to verify the prepare stage returns the expected query/model/URL/filter/min-threshold, keeps `count_embeddings=0` before save, and preserves filtered post-helper behavior (`SEMANTIC_FILTERED_LOCK_SCOPE_HARNESS query=semantic-filtered-lock-scope-query model=semantic-filtered-lock-scope-model results=1 activities=1 ...`). Existing filtered helper tests remain in place: `test_controlled_semantic_filtered_search_stub_uses_temp_db_embeddings`, `test_controlled_semantic_filtered_search_stub_respects_activities_and_min_similarity`, and `semantic::tests::test_filtered_search_respects_min_similarity`. Gates: `git diff --check` PASS; no CR-at-EOL in touched files; `cargo fmt --check` PASS; focused existing filtered stub PASS; focused activities+min_similarity PASS; focused new prepare-input PASS; focused `test_filtered_search_respects_min_similarity` PASS; full Rust gate PASS `46 passed / 0 failed`. Real AppData remained unchanged before/after (Roaming DB size `1482752`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, no WAL/SHM; Local DB absent both times). No active frontend component caller was found for filtered search beyond the API export; runtime IPC `semantic_search_filtered` **NOT RUN**; frontend/API `min_similarity` control **NOT IMPLEMENTED / NOT VALIDATED**; Ollama real **NOT RUN**; Tauri runtime/UI **NOT RUN**; `semantic_index_all` **NOT RUN / DEFERRED**; `create_semantic_playlist` **NOT RUN / DEFERRED**; temp dirs were **NOT manually deleted** (new harness dirs auto-cleaned; pre-existing `ytm-free-db-override-test-*` dirs left untouched). No production-ready claim is made.

**Step-6M.7 controlled create_semantic_playlist lock-scope split (2026-07-10, branch `fix/semantic-playlist-lock-scope` from main `ec4964a2ce9cce6a67d37ba5485e6c1216310c93`):** `create_semantic_playlist` no longer holds `state.db.lock().await` across `OllamaClient::embed_single(...).await`. `src-tauri/src/lib.rs` now adds a pre-embedding DB-only stage, `create_semantic_playlist_prepare_embedding_input_db_helper`, plus `SemanticPlaylistPrepareInput`; the helper reads settings from a real `Database`, enforces `semantic_search_enabled`, and returns the query together with `ollama_url`, `embedding_model`, and `playlist_name` without calling Ollama and without creating a playlist. A scoring-from-embedding DB-only helper, `create_semantic_playlist_scored_from_embedding_db_helper`, takes a pre-computed query embedding, reads all stored track embeddings, computes cosine similarity, filters by the existing threshold (`> 0.3`), sorts descending, truncates to 50, and resolves surviving track IDs into `Vec<SemanticSearchResult>` — preserving the exact scoring/sorting/truncation behavior that was previously inline in the wrapper. The existing save helper `create_semantic_playlist_from_scored_results_db_helper` remains unchanged and is still used for playlist creation and track linking. The public `create_semantic_playlist` IPC signature is unchanged. The wrapper now operates in three lock-scoped stages: (1) short-lived DB lock for prepare, (2) `embed_single().await` without DB lock, (3) reacquired DB lock for scoring + save. New focused harness `test_controlled_semantic_playlist_prepare_embedding_input_uses_temp_db_settings` uses temp `YTM_FREE_DATA_DIR`, synthetic settings (`semantic_search_enabled=true`, synthetic Ollama URL/model), synthetic query `semantic-playlist-lock-scope-query`, synthetic playlist name `semantic-playlist-lock-scope-playlist`; it verifies the prepare helper returns the expected query/model/URL/playlist_name, keeps `count_embeddings=0` and `get_playlists()` empty (harness print: `SEMANTIC_PLAYLIST_LOCK_SCOPE_HARNESS query=semantic-playlist-lock-scope-query playlist=semantic-playlist-lock-scope-playlist scored=0 saved=0 ...`). New focused harness `test_controlled_semantic_playlist_scored_from_embedding_uses_temp_db_embeddings` seeds three synthetic tracks + three synthetic embeddings (`[1.0, 0.0, 0.0]`, `[0.8, 0.6, 0.0]`, `[0.0, 0.0, 1.0]`), uses query embedding `[1.0, 0.0, 0.0]`, verifies Gamma is filtered out (below 0.3 threshold), Alpha ranks first (sim=1.0), Beta second (sim=0.8), then exercises the existing save helper to confirm playlist creation and track ordering (harness print: `SEMANTIC_PLAYLIST_LOCK_SCOPE_HARNESS query=semantic-playlist-lock-scope-query playlist=semantic-playlist-lock-scope-playlist scored=2 saved=2 ...`). The existing focused harness `test_controlled_semantic_playlist_save_stub_uses_temp_db_results` still passes unchanged. Gates: `git diff --check` PASS; no CR-at-EOL in touched files; `cargo fmt --check` PASS after one minimal `cargo fmt` application; toolchain profile autoload succeeded (`VCToolsInstallDir` set, `WindowsSdkDir` set, `WindowsSDKVersion=10.0.26100.0`, `LIB` contains `10.0.26100.0`, `dbghelp.lib` present); `cargo test --no-run` PASS; focused existing save-stub PASS; focused new prepare-input PASS; focused new scored-from-embedding PASS; full Rust gate PASS `48 passed / 0 failed`. Active UI caller confirmed in `src/api.ts` (`createSemanticPlaylist` → `invoke("create_semantic_playlist", ...)`), but `create_semantic_playlist` runtime IPC **NOT RUN**; Ollama real **NOT RUN**; Tauri runtime **NOT RUN**; `semantic_index_all` **NOT RUN / DEFERRED**; `semantic_search` runtime IPC **NOT RUN**; `semantic_search_filtered` runtime IPC **NOT RUN**; `semantic_index_track` runtime IPC **NOT RUN**; `smart_playlist_match_library` runtime IPC **NOT RUN**; yt-dlp **NOT RUN**; AppData real **NOT MUTATED** (Roaming DB size `1482752`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, no WAL/SHM before and after; Local DB absent both times); temp dirs were **NOT manually deleted** (new harness dirs auto-cleaned; pre-existing `ytm-free-db-override-test-*` dirs left untouched); AGENTS.md **NOT TOUCHED** (untracked, observed only); untracked GDPR drafts **NOT TOUCHED**. No production-ready claim is made.


**Step-6M.9 controlled semantic_index_all helper-only lock-scope split (2026-07-10, branch `fix/semantic-index-all-lock-scope` from main `345bc765ad7bd16000acba2880958ac3b76ad053`):** `semantic_index_all` now has a DB-only prepare stage, `semantic_index_all_prepare_batch_db_helper`, returning `SemanticIndexAllPreparedBatch` (`ollama_url`, `embedding_model`, prepared tracks with `Track`, `embedding_text`, and the metadata needed for ANN rebuild) plus a DB-only post-embedding save helper, `semantic_index_all_save_embedding_db_helper`, which persists a precomputed embedding with the exact prepared `text_used` and returns the saved `TrackEmbedding`. The public IPC signature is unchanged. The wrapper now takes a short-lived DB lock only for batch preparation, runs `embed_single().await` without DB lock and without shared `ann_index.write().await`, reacquires DB lock briefly per successful save, accumulates ANN entries in a local `ANNIndex`, then swaps the rebuilt ANN into shared state under one short-lived write lock at the end. Existing `semantic_index_track` helpers remain unchanged and still pass their focused harnesses. New focused harnesses `test_controlled_semantic_index_all_prepare_batch_uses_temp_db_metadata` and `test_controlled_semantic_index_all_save_batch_uses_temp_db_embeddings` use temp `YTM_FREE_DATA_DIR`, synthetic settings/tracks/metadata/embeddings, verify prepare output, verify no embeddings are written during prepare, verify persisted save results after `Database::new()` reopen, and print `SEMANTIC_INDEX_ALL_LOCK_SCOPE_HARNESS ...`; note that `build_track_text` has no album field, so the new assertions validate only the actual title/artist/genre/mood/activity/keyword/tempo/decade inputs. Gates (profile-loaded PowerShell shell): `cargo fmt --check` PASS; `cargo test --no-run` PASS; focused existing index-track prepare PASS; focused existing index-track save PASS; focused new index-all prepare PASS; focused new index-all save PASS; full Rust gate PASS `50 passed / 0 failed`. Real AppData remained unchanged before/after (Roaming DB size `1482752`, SHA256 `3A3B255A6A7052E280928323038EA1CA395A5E223985DD853F4D11C969B1BC25`, no WAL/SHM; Local DB absent). Runtime IPC `semantic_index_all` **NOT RUN**; Ollama real **NOT RUN**; Tauri runtime **NOT RUN**; AppHandle/progress event delivery **NOT RUN**; frontend `semantic-index-progress` listener remains **NOT IMPLEMENTED / NOT VALIDATED**; temp dirs were **NOT manually deleted**; production-ready **NO**.

## What the project is

Personal-use desktop music player: Tauri 2.x shell, React 18 + TypeScript + Zustand + Tailwind frontend, Rust backend (SQLite via rusqlite, Axum streaming server on port 3456, yt-dlp for YouTube search/stream/download, Ollama client for ~97 local-AI features). Package manager: **npm** (package-lock.json committed). Repo lives at `C:\Users\gglig\.ytm-free` (yes, a dot-directory in the user profile — this is the real working copy, not a config dir).

## Verified working (2026-07-06)

| Claim | Evidence |
|---|---|
| TypeScript compiles, 0 errors | `npx tsc --noEmit` → exit 0 (2026-07-06, 4th session) |
| Frontend production build works | `npm run build` → vite built dist/ in 6.98s, 420.33 kB JS (2026-07-06, 4th session) |
| Frontend tests pass | `npm test` → 5 files passed, 32 tests passed (2026-07-06, 4th session) |
| Known flaky frontend test did not reproduce in the 4th session | Historical: `LibraryView.test.tsx` "handles 1000 tracks" can time out under full-suite load but passes in isolation. Current run: `npm test` → 32/32, so no isolation rerun needed. |
| yt-dlp installed | `yt-dlp --version` → 2026.02.04 |
| Node v22.22.2 / npm 11.7.0 / rustc+cargo 1.94.1 | `--version` commands |
| Rust tests pass in a normal PowerShell shell without manual `vcvarsall` (2026-07-08) | User-level env pin + `C:\Users\gglig\Documents\PowerShell\profile.ps1` bootstrap to SDK `10.0.26100.0`; then `cargo test --no-run` → compiled successfully and full `cargo test -- --test-threads=1` → `34 passed, 0 failed`. |
| Rust tests pass with SDK pin (2026-07-06, 4th session) | `vcvarsall.bat x64 10.0.26100.0` then `cargo test db::tests` in `src-tauri/` → 8 passed; full `cargo test` → 28 passed, 0 failed. Only existing dead-code warnings. (Plain `cargo test` without the pin still fails — see BROKEN table.) |
| Production Tauri build succeeds with corrected baseline order and SDK pin (2026-07-06, 10th session) | `npx tsc --noEmit` → exit 0; `npm test` → 32/32; `npm run build` → rebuilt `dist`; SDK-pinned `cargo test` → 28 passed; SDK-pinned `npm run tauri build` → exit 0, built `target\release\ytm-free.exe`, MSI, and NSIS bundles. |
| Tauri dev runtime starts with SDK pin (2026-07-06, 5th session) | `vcvarsall.bat x64 10.0.26100.0` then `npm run tauri dev` → Vite ready at `http://localhost:5173`, `target\debug\ytm-free.exe` running with responding window title `YTM Free`, `Get-NetTCPConnection` shows `127.0.0.1:3456` listening, and `curl.exe -i http://localhost:3456/health` → `HTTP/1.1 200 OK` / `OK`. |
| Minimal app-local stream route works for a sampled search-result ID (2026-07-06, 7th session, branch `fix/stream-audio-url-resolution`) | `npm run tauri dev` under `vcvarsall.bat x64 10.0.26100.0`; `curl.exe -i http://localhost:3456/health` → `HTTP/1.1 200 OK` / `OK`; `curl.exe -i http://localhost:3456/stream/YXZH-eBtmqQ` → `HTTP/1.1 307 Temporary Redirect` with a `googlevideo.com/videoplayback` location. |

## Verified BROKEN (2026-07-06)

| Problem | Evidence |
|---|---|
| **Underlying Windows SDK `10.0.28000.0` defect still exists, but the normal PowerShell workflow is no longer blocked (re-verified 2026-07-08)** | Raw/unbootstrapped shell evidence remains: plain `cargo test` previously failed with `LNK1181: cannot open input file 'dbghelp.lib'`. New evidence: after the user-level env pin + `C:\Users\gglig\Documents\PowerShell\profile.ps1` bootstrap to SDK `10.0.26100.0`, `cargo test --no-run` compiles and full `cargo test -- --test-threads=1` passes `34/34` from a normal PowerShell shell without manual `vcvarsall`. |
| Root cause confirmed unchanged: Windows SDK 10.0.28000.0 is a **partial install** — `um/x64` has 115 libs (no `dbghelp.lib`) vs 481 in SDK 10.0.26100.0 (which has it); the toolchain auto-selects the newest SDK | `ls ...\10.0.28000.0\um\x64\dbghelp.lib` → No such file; `ls ...\10.0.26100.0\um\x64\dbghelp.lib` → exists. Counts: 28000 = 115 libs, 26100 = 481 libs (re-verified 2026-07-06). |
| ~~NEW blocker — disk space (verified 2026-07-06, 2nd session)~~ **RESOLVED 2026-07-06 (3rd session):** C: now has 11.2 GB free; `cargo test` with the SDK pin completed the build (`Finished in 35.98s`) and ran all 28 tests. | (Was) `fsutil volume diskfree C:` → 354.8 MB free; `du -sh src-tauri/target` → 4.7 GB. (Now) `fsutil volume diskfree C:` → Total free 11,994,079,232 bytes (**11.2 GB**). Build artifacts: `src-tauri/target` 4.7G, `node_modules` 171M, `dist` 461K. | 8th-session recheck (2026-07-06): C: free dropped to **8.70 GB** (9,342,238,720 bytes); `src-tauri/target` grew to **10.62 GB**. Step 3A production build blocked by the 15 GB disk gate — see Step-3A evidence above. |
| **SDK-pinned `cargo test` is not clean-workspace safe after deleting `dist` (verified 2026-07-06, 9th session)** — with `src-tauri/tauri.conf.json` `frontendDist` set to `../dist`, `tauri::generate_context!()` panics if `dist/` is absent, so the Step 3A baseline order `cargo test` before `npm run build` fails after approved artifact cleanup. | Post-cleanup preflight on main: C: `24.03 GB` free, `src-tauri/target` absent, `dist` absent, SDK pin active (`WindowsSDKVersion=10.0.26100.0`). Then `cd src-tauri && cargo test` → exit 101: `The frontendDist configuration is set to "../dist" but this path doesn't exist`. `npm run tauri build` was not run; the failed cargo invocation recreated `src-tauri/target` to about `2.05 GB` while `dist` stayed absent. |
| ~~NEW — 8 real `db::tests::*` failures (verified 2026-07-06, 3rd session)~~ **RESOLVED 2026-07-06 (4th session):** root cause was the version-0 baseline collision in `run_migrations`; fixed by treating an empty `schema_migrations` table as baseline `-1`, so migration 0 runs on fresh databases. | Before fix: `cargo test` (SDK-pinned) → `test result: FAILED. 20 passed; 8 failed`. After fix: `cargo test db::tests` → `8 passed; 0 failed`; full `cargo test` → `28 passed; 0 failed`. |
| ~~Step 2B minimal runtime smoke failed in the stream path (verified 2026-07-06, 6th session)~~ **RESOLVED on branch `fix/stream-audio-url-resolution` (7th session):** the app-local stream route no longer returns 500 for sampled ID `YXZH-eBtmqQ`. | Before fix: `curl.exe -i http://localhost:3456/stream/YXZH-eBtmqQ` → `HTTP/1.1 500 Internal Server Error`; root cause was strict `yt-dlp -f bestaudio -g`, which fails when no audio-only format is exposed. After fix: `get_audio_url` uses `bestaudio/best`; runtime smoke returned `HTTP/1.1 307 Temporary Redirect` with a direct `googlevideo.com/videoplayback` URL. |

Fix procedure: see `docs/RECOVERY_PLAN.md`. **Step 1 part 1 (dbghelp.lib) is now resolved for the normal PowerShell workflow on this machine** by a user-level env pin plus the shared PowerShell bootstrap at `C:\Users\gglig\Documents\PowerShell\profile.ps1`, which auto-selects SDK `10.0.26100.0` without manual `vcvarsall`. The OS-level root cause still exists: SDK `10.0.28000.0` remains partial on disk, so repairing/removing it in Visual Studio Installer is still the cleaner machine-wide fix, especially for non-PowerShell or env-less tools. **Step 1 part 2 (disk space) — RESOLVED 2026-07-06 (3rd session):** C: now has 11.2 GB free and the build completed. **Step 1 part 3 (db migration bug) — RESOLVED 2026-07-06 (4th session):** with the SDK pin, the 28 Rust `#[test]` functions (db.rs: 8, semantic.rs: 5, spotify_import.rs: 7, ollama/client.rs: 6, ollama/prompts.rs: 2) now pass when `dist/` already exists. Net: in the normal PowerShell workflow, `cargo test` is now **PASS** when the configured `frontendDist` path exists.

**Step-6M.12 frontend semantic progress listener integration — PR #4 squash-merged (2026-07-11, main `ae3978d3aa0290525608e8ca3bae2a79aed6840a`):** semantic indexing progress UI is now **IMPLEMENTED / STATIC/UNIT VERIFIED / NOT RUNTIME VERIFIED**. Preflight confirmed local `main` == `origin/main` == `ae3978d3aa0290525608e8ca3bae2a79aed6840a`, GitHub PR #4 state=MERGED with mergeCommit SHA matching, feature branch remote deleted. Scope: exclusively frontend in `src/components/settings/SemanticSettings.tsx` + test in `src/__tests__/SemanticSettings.test.tsx`; no Rust, no backend helpers extracted, no Ollama, no download. Baseline gates (main): **Focused test PASS** (`5 passed` in SemanticSettings.test.tsx); **TypeScript PASS** (`npx tsc --noEmit` exit 0); **All tests PASS** (`41 passed` frontend total, up from 32 due to cumulative harness additions from parallel locked-scope branches); **Build PASS** (`vite built in 5.59s`); **Whitespace PASS** (`git show --check HEAD` and `git diff --check` both exit 0). Changes: `SemanticSettings` component now listens to the existing Tauri IPC event `semantic-index-progress` (emitted by backend `semantic_index_track` and `semantic_index_all` during indexing), displays real-time progress as percentage and indexed/total counts plus current track being indexed, with lifecycle-safe cleanup: listener is cleared on successful completion, error, index-replace, component unmount, and after a 30-second safety timeout if embedding late-resolves. Frontend event binding verified by unit tests for progress-update reception, error-state handling, cleanup on success, unmount cleanup, and late-resolve safety. The semantic-index-progress event itself originates from the existing backend `semantic_index_track` and `semantic_index_all` commands and was integrated in prior fix-branches (6M.1–6M.9); this step verifies the frontend UI consumes it. Unverified: Tauri runtime event delivery from backend to frontend; live Ollama embedding during indexing; actual semantic search with indexed embeddings; full e2e app flow; production-ready status. Repo state after gate pass: main clean, no staged changes, branch `docs/sync-6m12-state` created for this documentation-only step, no code modification, production-ready=**NO**.

## Never verified (open since project start)

- Full end-to-end app flow (search -> stream -> download -> playlist -> restart -> data persisted) remains **unrun**. Step-6A preflight mapped a controlled Step-6B plan; Step-6B.1-6B.3 verified release startup, `/health`, and no-follow stream redirect under temp `YTM_FREE_DATA_DIR`, but runtime search was not run because it needs a UI/Tauri IPC harness. Step-6B.4 now verifies import persistence at Rust level with synthetic CSV input and a temp DB, but direct runtime UI/Tauri-IPC import plus runtime search are still not exercised end-to-end. Step-6B.5 verified baseline DB persistence across a controlled stop/relaunch under temp `YTM_FREE_DATA_DIR`; Step-6B.5b verified at Rust level that synthetic tracks/playlists survive a `Database::new()` reopen against the same temp `YTM_FREE_DATA_DIR`; Step-6B.6 verified Rust-level playlist-link removal persists after a `Database::new()` reopen; Step-6B.7 verified Rust-level local DB metadata filters persist after a `Database::new()` reopen. Step-6C.2 verified no-state CSV IPC wrappers at Rust level, Step-6D.1 verified stateful playlist DB helpers at Rust level, Step-6E.1 verified AI/semantic DB-cache/status helpers at Rust level, and Step-6F.1 verified the semantic search DB/scoring helper with precomputed synthetic embeddings at Rust level. Step-6M.1 through Step-6M.11 verified backend lock-scope splits for embedding isolation. Step-6M.12 verified frontend `SemanticSettings` listens to and displays the existing `semantic-index-progress` Tauri IPC event with lifecycle-safe cleanup; runtime event delivery, live Ollama, semantic search runtime IPC remain unverified. Runtime UI/Tauri-IPC import, delete, restart persistence, search, AI/Ollama remain unverified. Download and full e2e remain unexecuted.
- Running the built release artifacts through broader user flows remains only **partially verified**. The production build itself is verified (Step 3A). Step 3C verified narrow isolated release-runtime smoke with `YTM_FREE_DATA_DIR`. Step 5A verified NSIS install -> installed `/health` runtime -> uninstall cleanup. Step 5B verified MSI install -> installed `/health` runtime -> uninstall cleanup. Step 6B.1-6B.3 verified direct release exe startup, `/health`, and no-follow stream redirect under a fresh temp DB. Step-6B.4 now verifies import persistence via a Rust-level synthetic harness; Step 6B.5 verified baseline restart persistence; Step 6B.5b verified synthetic-state restart persistence at the Rust `Database` reopen level; Step 6B.6 verified playlist-link removal persistence at the Rust `Database` reopen level; Step 6B.7 verified local DB metadata filter/search persistence at the Rust `Database` reopen level; Step 6C.2 verified no-state CSV wrappers; Step 6D.1 verified stateful DB helper wrappers; Step 6E.1 verified AI/semantic DB-cache helpers; Step 6F.1 verified the semantic search DB/scoring helper with precomputed synthetic embeddings. Direct runtime UI import, runtime UI search, runtime UI restart persistence, runtime UI delete, runtime AI/Ollama, runtime semantic search IPC, and full controlled e2e remain unexecuted.
- Download behavior remains separately unverified and is **not currently isolated by `YTM_FREE_DATA_DIR`**: `download_track` writes to the real Windows audio/download location via `dirs::audio_dir()` / `dirs::download_dir()`, so download needs its own controlled plan before it can be exercised safely.
- Ollama-dependent features against a live Ollama instance remain unverified. Step-6E.1 verified only DB-cache/status helpers in a temp DB and did not run Ollama, network, embeddings generation, semantic search, or runtime UI. Step-6F.1 verified only the post-embedding semantic search DB/scoring helper with a synthetic precomputed embedding; it did not run Ollama, runtime IPC semantic search, network, or UI. Step-6G.1 verified only the semantic filtered-search DB fallback helper with a synthetic precomputed embedding; it did not run Ollama, runtime IPC semantic filtered search, network, UI, activities filtering, or min_similarity behavior. Step-6I verified only the semantic playlist save DB-only helper with precomputed synthetic scored results in a temp DB; it did not run Ollama, `create_semantic_playlist` runtime IPC, network, or UI.

## Git state (2026-07-11)

- **`main` is the real trunk.** Local `main`, `origin/main`, and GitHub's default branch are all aligned to `ae3978d3aa0290525608e8ca3bae2a79aed6840a` (squash-merged PR #4, dated 2026-07-11 14:48:18 UTC).
- The old default-branch trap is **resolved**: GitHub defaults to `main`, and local `origin/HEAD` points to `origin/main`.
- Remote feature branch `feat/semantic-index-progress-listener` was deleted after squash-merge into main.
- 4 merged-and-stale feature branches still remain (debt/cleanup-sprint, faza-3/*, faza-4/*, phase-2-frontend-bugs) — squash-merge workflow, so their commits are not ancestors of main. `phase-2-frontend-bugs` is no longer the default branch.
- Untracked, uncommitted work: `gdpr-compliance-audit-report.md`, `docs/GDPR_REMEDIATION_PLAN.md` (status: DRAFT, awaiting owner approval), `docs/plan-remediere-gdpr-complete.md`, `.omx/` (agent session state — do not commit).
- `Cargo.lock` is **gitignored** (see .gitignore). For an application this breaks reproducible Rust builds — known debt, decision pending.
- `Spotify/*.csv` (personal listening exports) are tracked in git — flagged by the GDPR audit.

## Known-stale documents — do not trust their status claims

| Doc | Stale claim | Reality |
|---|---|---|
| `docs/FINAL_STATUS_97_FUNCTIONS_COMPLETE.md` (2026-02-14) | "Build Status: ✅ Production-Ready" | Production build has never succeeded; plain Rust builds still need the SDK pin or SDK repair |
| `docs/IMPLEMENTATION_SUMMARY_COMPLETE.md` (2026-02-14) | "Production-Ready" | Same |
| `docs/CHANGELOG.md` (2026-02-14) | "Status: Production-Ready ✅" | Same |
| `README.md` (2026-02-14) | "92 Tauri commands" | `grep -c '#\[tauri::command\]' src-tauri/src/lib.rs` → 112 |
| `.github/agents/GaborAI.agent.md` | — | Unfilled template, no content |

`docs/ROADMAP_STATUS.md` (2026-04-30, in Romanian) is the most honest status doc and matches command evidence.

## How to update this file

Run `docs/VERIFICATION_PROTOCOL.md`, record results in an evidence ledger (`docs/EVIDENCE_LEDGER_TEMPLATE.md`), then update the tables above with new dates. Never mark something working without the command output in hand.
